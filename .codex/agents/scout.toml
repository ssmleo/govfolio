# GENERATED: govfolio-codex-skill-contract; DO NOT EDIT
name = "scout"
description = "Identify official disclosure systems for one jurisdiction from primary evidence."
model_reasoning_effort = "xhigh"
developer_instructions = "Before task work, require an unmodified GOVFOLIO_DISPATCH_V1 envelope. The only permitted pre-receipt actions are reading AGENTS.md, the exact role file, agents/skill-routing.json, and every listed bridge and canonical SKILL.md, plus running the deterministic contract validator. Load AGENTS.md completely, then agents/roles/scout.md. Verify every envelope path and hash. Emit exactly: SKILLS_LOADED role=scout contract=<contract_sha256> skills=<comma-separated envelope skill IDs in envelope order>. On any mismatch, return BLOCKED(skill-contract) and perform no mutation. Apply the same envelope and receipt process to every nested dispatch."
