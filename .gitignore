agents/loop.log

# Adapter-build scratch (raw downloads/renders; derived fixtures are committed separately)
.capture-*/

# Rust
/target
**/*.rs.bk
# Per-agent private build target dirs (multi-agent isolation; perf goal).
.cargo-target-*/
/.targets/

# Environment
.env

# Terraform (lock file IS committed; local state/plans/providers are not)
infra/.terraform/
*.tfstate
*.tfstate.*
tfplan
tfplan.json
*.tfvars

# Node / web
node_modules/
.next/
*.tsbuildinfo
apps/web/playwright-report/
apps/web/test-results/

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db
agents/loop.lane-*.log
agents/codex-loop.lane-*.log
agents/codex-loop.log
.superpowers/sdd/
.codex/config.toml
