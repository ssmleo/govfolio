# Orchestrator journal (one line per iteration)
2026-07-04 | env | claude 2.1.201 (>=2.1.198 floor OK); host: no admin/Docker/WSL, no gcc; portable PG 16.14 @localhost:5433 (zonky) substitutes docker-compose locally; CI covers postgres:16 service | blockers: none
2026-07-04 | 001/T1 | workspace+lint law+smoke green, commit 4011472 (rust-builder, verified by orchestrator) | deviation: adapters glob deferred to first adapter crate (cargo empty-glob error)
2026-07-04 | 001/T2 | ci.yml 3 jobs, run 28718998677 success, commit 98a5147 | none
2026-07-04 | 001/T3 | migrator+compose+env, ignored-test convention, commit f642922, CI green | agent stalled on own CI watcher; orchestrator verified directly
2026-07-04 | 001/T4 | domain primitives + GoldCandidate schema snapshot, commits 4bff22b+d9a5b94, CI green ×2 | first attempt killed by founder mid-red; resumed from partial state. Skills incident: builders missed agents/skills/ (dispatch prompts now carry explicit SKILL.md paths)
2026-07-04 | skills | founder-side install detected 18:10-18:12: .agents/skills/{rust-async-patterns,rust-best-practices,tdd-rust} + skills-lock.json (wshobson/agents, apollographql/skills, rtk-ai/rtk) — bypasses 019 gateway (A3 vendored-pin+audit); left untracked/inactive, agents instructed to ignore; recommend routing through goal 019 | awaiting founder confirm
2026-07-04 | 001/T5 | 0001_core.sql §4.2 verbatim + support tables, 23514 tests, sqlx 0.8→0.9 align, commit ad481d4, CI green | deviation: build.rs rerun-if-changed added (embedded migrator staleness)
2026-07-04 | 001/T6 | fingerprint canonical-JSON sha256, cross-process determinism probed, commit 2a182d1, CI green | none
2026-07-04 | 001/T7 | adapter trait+RunCtx+conformance harness+fixture_fake (pass+broken case), commit 93c8991, CI green (incl. founder's 659310f guardrail gates) | none
2026-07-04 | policy | root CLAUDE.md + docs/decisions/automation-policy.md: ALL human gates lifted → expected.*.json auto (2nd-model cross-check), goal 001 BLOCKED(human) superseded | note: single-model fleet → cross-check = independent blind agent, flagged as approximation
2026-07-04 | 001/T8a | us-house regime doc, evidence-pinned, 4 fixtures sha256'd, commit 1c62dcd (spec-writer) | KEY: PTR amendment=FilingType P new DocID (not A); supersession link impossible → review_task. Open: blank-owner=self assumption flagged; statute text unarchived
2026-07-04 | HALT | founder steering: stop after T8a for PC restart. Next: T8b fixture capture + independent expected.*.json (test-designer), T8c adapter impl, T8d auditor cross-check, then T9-T11 | resume: restart PG (see goal 001 env note), re-run loop
2026-07-04 | 001/T8b | fixtures ×4 + independent expected.*.json (test-designer), sha pins zero-drift, 12 rows dual-derived, MANIFEST conventions, commit 8a6c9a6 | CI went red (fixtures dir vs adapters glob) — fixed by T8c scaffold c6dceb7
2026-07-04 | host | founder-requested: host-quirks agent — gcc(WinLibs)+cargo PATH verified fixed, pg-local.ps1, runbook docs/runbooks/dev-host-windows.md, commit 8fb1b06; memory updated | none
2026-07-04 | 001/T8c | us_house adapter conformance 4/4 (rust-builder), commits c6dceb7+5681073, CI green; no fixture edits (verified) | surfaced: serde_json float_roundtrip harness fix; details-type placement → adjudicated T8d
2026-07-04 | AUDIT 001/T8d | PASS — 12/12 rows re-derived via independent 3rd extractor (visual render), 0 mismatches; PDFs re-hash to §7 pins; fixtures single-commit 8a6c9a6 predates adapter; invariants 2/3/6/7/8/10 clean; conformance 4/4 + full block + ignored sqlx re-run green; allocator ceilings OK (4/4/6 slots); Pelosi UBER `D:` = filer copy-paste (verbatim in PDF); details-type placement adjudicated adapter-local (design §5.1+§9 over §4.3), regime doc §5 amended | notes: §3.6 "46-code legend" vs 48 bucketed codes unverifiable (E2 snapshot uncommitted); E2/E3/E9/E10 evidence snapshots missing from docs/regimes/us-house/evidence/ despite §8 same-PR note — hygiene work item, non-blocking
2026-07-04 | 001/T9 | e2e local runner idempotent (Bronze/Silver/Gold/outbox same-txn, run2 inserts nothing across 15 tables), migration 0002 expand-only, roster seam closed, commit 39d591b, CI green | none
2026-07-04 | 001/T10 | /v1 records+timeline, openapi.json drift-gated in CI, contract tests vs emitted schema, ULID cursor paging, commit e9bdf95, CI green | none
2026-07-04 | 001/T11 | promote.rs confirm/edit/reject, supersede-never-update locked byte-level (row::text probe), commits 546fa94+edc5240, CI green | one accidental founder stop, redispatched clean
2026-07-04 | GOAL 001 | COMPLETE — full acceptance block green verified by orchestrator in-session (fmt/clippy/73 tests/conformance 4/4/12 ignored sqlx on PG 5433) + CI green at head; ticked in 000-INDEX | next: 015 coverage factory
2026-07-04 | GOAL 015 | setup COMPLETE — migration 0003 registry columns (expand-only, guardrail green), 3 fail-closed validators (sources/survey/manifest) w/ derived schemas appended to source-exploration.md, commit c9fa7b8, CI green; verified by orchestrator (108+15 tests, validate-manifest us_house OK, validate-survey fails closed as designed) | one accidental founder stop, redispatched. Standing loop = orchestration 2d from E2
2026-07-05 | GOAL 016 | COMPLETE — E1 reference frozen (17 sha pins, E1.lock.json), 7 mechanical scorers (zero LLM-judge), thresholds 1.00 documented (NOT_APPLICABLE=BLOCKING), epoch-gate bin honest (E2 BLOCKED: scout/surveyor/sampler refs absent), role_evals 11/11, commit 1da4a91, CI green; orchestrator re-ran acceptance | hygiene work items: .gitattributes -text for pinned artifacts (autocrlf re-clone landmine); E2/E3/E9/E10 evidence snapshots still unarchived; us-house vs us_house id tidy
2026-07-05 | GOAL 019 | COMPLETE — Phase A scout: 5 imports pinned+vendored+screened (impeccable@582f23e, rust-best-practices@7df6a60, rust-async-patterns@5cc2549, ts-advanced-types@5cc2549, frontend-design@9d2f1ae), 2 fail-closed skips (ts-react-reviewer no-license, ts-expert ambiguous → bespoke-planned); .agents/ installs byte-match canonical (supply chain clean). Phase B line-audit WAIVED by founder 2026-07-05 in chat (recorded in skill-imports.md); ultracode audit workflow launched then stopped on founder call — cost note: ~0 (killed early, first-of-class gauge inconclusive). Phase C: waiver logged, superpowers merges (chassis DoD + rust-tdd distill), ARMED flipped (impeccable DOCS-ONLY), acceptance greps green, commits ea986e1+961edf8, CI green | typescript-expert source pick = open founder note (non-blocking)
2026-07-05 | GOAL 020 | 3/4 — infra/ modules (SQL PITR, GCS versioned+prevent_destroy, Cloud Run x3, Tasks queues, Scheduler paused stubs, Secret shells, least-priv IAM, WIF keyless CI), deploy.yml (guardrailed: plan→check-tf-plan→apply), runbook bootstrap; terraform 1.15.7 installed user-scope; fmt+validate green local+CI, commit 5064d4c | HALT(interactive): plan needs ADC — founder: `gcloud auth application-default login` (or type `! gcloud auth application-default login` in chat). Hygiene note: .agents/ gcloud skills untracked but runbook-referenced — regularize in future pass
2026-07-05 | GOAL 021 | COMPLETE — scanned paper-PTR fixture 9115811 (test-designer, dual-render transcription, text layer provably absent), AnthropicExtractor (forced tool-use, schema-constrained, haiku-4-5 primary + sonnet-5 cross-check, key-redacted transport), sha+tag+model cache (file tier primed from ground truth + pg tier migration 0004), cross-check mismatch → review_task+freeze, E1 lock superseded v2 w/ provenance trail; conformance 5/5 OFFLINE, extraction 16/16, role_evals 11/11, workspace 152, CI green; commits 77740d8+2dfed7a; orchestrator re-verified all | deviation: cross-check threshold $500,001 per SAF §6.3 (not $1M); live API test ignored until ANTHROPIC_API_KEY provisioned (secret-manager shell exists from 020)
2026-07-05 | GOAL 030 | COMPLETE — ONE RecordFilter grammar (core, snapshot-committed) shared by /v1/records + matcher + alert_rule.filter contract; migration 0005 alert_rule+delivery; dispatcher exactly-once proven under forced redelivery (dedup unique + ON CONFLICT); HMAC-SHA256 webhooks (RFC vector test), SMTP config-gated (dead-letters loudly when unconfigured), retry→dead DLQ, digest one-summary-per-rule; rules CRUD /v1 (schema-strict 400s), openapi regen drift-clean; worker alerts 8+5, workspace 169, CI green, commit 21971ec; orchestrator re-verified | deviations logged in goal (CASCADE ledger trade-off, per-channel-type rule constraint)
2026-07-05 | GOAL 040 | COMPLETE — 040a: /v1 read surface (politicians list/profile, record detail + transitive supersession CTE provenance, jurisdictions+regimes scorecard, search, strong-ETag middleware w/ 304s), contract 10/10, commit 75ccb91. 040b: pnpm workspace + Next.js SSR (profile /p/[id], record /r/[id] w/ provenance+verification badges+supersession links, jurisdictions scorecard, search, sitemap.xml, CDN cache headers), generated openapi-typescript client only, zero any, CI web job activated w/ TS drift gate, commit e87a342, CI green; orchestrator re-ran acceptance: vitest green + playwright 11/11 vs live seeded stack | next: 041 reviewer UI
2026-07-05 | GOAL 041 | COMPLETE — 041a admin /v1 (ranked queue, task detail w/ extraction pre-review context, resolve→promote.rs thin door, migration 0006 review_audit one-row-per-attempt, 409 on double-resolve), commit 8722fb6, CI green (watch-glitch false red, jobs 4/4 success). 041b reviewer UI ((reviewer) route group noindex, queue, side-by-side extracted↔Bronze iframe+sha, actions confirm/edit/reject via server action → resolve endpoint only, audit panel), vitest 44/44 + playwright reviewer flows 4/4 (suite 15/15), commit cea2b05, CI green; orchestrator re-ran both acceptance commands verbatim | notes: archived-copy serving post-020-apply; non-record tasks render w/o forms (fail closed)
2026-07-05 | GOAL 050 | COMPLETE — migration 0007 (accounts/keys/usage/subscription), sha256-only key storage w/ immediate revocation, 24h free delay in ONE query place (core query slot $11 on filing.discovered_at — knowledge time, honestly), quota 60/day free → 429 w/ exact usage ledger, stripe seam (HMAC-verified webhooks, mock-proven, config-gated live), billing-sync + snapshot bins (CC BY factual note only), review surface admin-token-gated + alert rules paid-only (back doors closed); tiers 8/8, workspace 182+44, CI green, commit c103f91. Follow-up regression (reviewer UI 401s) caught by orchestrator re-running 041 acceptance → fixed same session 9f6a337, reviewer+full e2e 15/15, CI green | founder TODOs: price values, real signup, ADMIN_TOKEN + GOVFOLIO_ADMIN_TOKEN env pair for prod
2026-07-05 | AUDIT 060 | PASS — 40/40 rows re-derived (all 4 fixtures; mandate floor 6) via independent hand-rolled tag-tokenizer + SAF §3.2-§3.5/§4/§5.1/§6 tables only (no DOM lib — disjoint from adapter's scraper/html5ever), 0 mismatches key-set-strict on silver+gold: amendment_number=1 x18 (incl. Sale (Full) #7 50001.00/100000.00), Sale (Partial) x3 (ORCL/NVDA, partial_sale true), Child-owner bond (dependent, ticker null, detail sub-line, 0.9800000190734863), name-embedded tickers SPYM/FTGC/ACN (ticker_raw null, asset_name verbatim); §3.7 integrity re-computed independently on all 4 docs; fixtures single-commit f07f5eb 09:30 predates adapter 4dc47c1 10:34, diff f07f5eb..HEAD on fixtures/ empty, worktree clean; 4/4 input.html re-hash to §7/MANIFEST pins (repo blob AND worktree — no autocrlf damage), evidence archives sha-name-consistent + byte-equal to fixtures; invariant sweep clean: instrument_id always None (ticker rides details only), asset_description_raw verbatim, bands→Decimal→decimal-string ValueInterval (no floats in money), notified_date always None, unknown band/owner/type/filer/sha hard-reject, unobserved "Over $1,000,000" outside grammar per §3.4, amendment supersedes NULL (no linkage guessing), StubExtractor freezes needs_llm_extraction (never silent rows), politeness concurrency 1 + 2s + contact (§2.5 From-header deviation documented); spot-checks: confidence 1.0 float literal everywhere (zero integer-1), S&amp;P entity-decoded in both 18-row expecteds; conformance us_senate 4/4 re-run green | none blocking
2026-07-05 | GOAL 060 | COMPLETE (E1 us_senate) — regime doc w/ verified session-dance + Akamai constraint (94f0dd6), 4 fixtures independently authored w/ raw-byte pins (f07f5eb), adapter conformance 4/4 incl. amendment/partial-sale/child-bond/name-embedded-ticker (4dc47c1), auditor cross-check PASS 40/40 rows 0 mismatch (70fb61c); pipeline adapter.rs gained post_form+cookie-store for session dance; us_house 5/5 + workspace 211 unregressed, CI green throughout; orchestrator re-ran conformance + full block | follow-ups recorded in goal: (1) live fetch needs browser-TLS (wreq/sidecar) vs Akamai; (2) stg_us_senate + RunnerBinding for e2e publish. Parallelization eval delivered to founder: front legs (spec+fixtures) fan out per-jurisdiction; build leg serializes on conformance.rs+Cargo.lock+branch
2026-07-05 | AUDIT 061 | PASS — 5/5 fixtures re-derived silver+gold key-set-strict both directions via independent node-stdlib derivation from SAF §3.1/§3.4/§3.5/§3.6/§4/§5.1/§6 tables only (disjoint from adapter serde/fields.rs), 0 mismatches: 15475 shareholding (ii) → open-ended 70000.00–NULL GBP (R2a, owner self, equity), 15923 donation exact 3400.00–3400.00 GBP (R1; summary "£3,400.00" suffix NOT parsed), 15854 land value NULL + IsSoleOwner=false→joint (R4, real_estate), 15914 parent value NULL owner self, 15915 child parentInterestId=15914 exact 500.00 GBP + HoursWorked "5.00" correctly non-money (currencyCode-keyed) + Gŵyl UTF-8 verbatim; notified_date=registrationDate genuinely discriminated (reg≠pub on 15923/15914/15915, never publishedDate); confidence float-literal byte-scan 10/10 = 1.0 (zero integer-1); integrity: fixtures single-commit c95a437 (src touch = scaffold Cargo.toml + doc-only lib.rs) predates adapter 648e767, no fixture touch after, 5/5 input.json worktree==HEAD-blob==MANIFEST==SAF §7 pins, .gitattributes -text covers fixtures+evidence (inputs single-line i/none — conversion-immune); invariant sweep clean: instrument_id always None, record_type interest + side/transaction_date/as_of_date None on every row, money Decimal→decimal strings end to end (no floats in money; f32 confidence only), deny_unknown_fields drift freeze at top level + field grammar w/ open name vocabulary, hard rejects verified (R2c unknown threshold, unmapped currency, non-string Decimal, mixed-donor currencies, id mismatch, unknown category, non-Commons house/type, empty summary, bad dates, unknown sha/member conformance tables), owner grammar gaps → unknown never guessed (HeldOnBehalfOf non-null/missing, IsSoleOwner missing), politeness concurrency 1 + 2s + identified contact (From-header = recorded runner follow-up); details schema snapshot drift-gated (tests/details_schema_snapshot.rs) + registry arm fails closed; GoldCandidate::validate() accepts interest rows w/ open-ended high=None (harness validates every candidate at conformance.rs:185 + adapter unit test asserts); acceptance re-run by auditor: uk_commons_register 5/5, us_house 5/5, us_senate 4/4, fixture_fake 1/1, cargo test -p uk_commons_register 30/30 | none blocking; surfaced (not followed): untracked agents/monitor.sh byte-matches in-repo blob d9f1f55; .agents/ + skills-lock.json untracked-only (pre-existing, cf. 019/020 notes)
2026-07-05 | GOAL 061 | COMPLETE (E1 diversity seed uk_commons_register) — FIRST non-transaction adapter (record_type interest, value_precision categorical) + first pure-JSON-API source (interests-api.parliament.uk, no scrape/no LLM). Regime doc w/ value rules R1-R4 incl. open-ended value_high NULL + .gitattributes -text repo-wide CRLF-pinning fix (16337a6); 5 fixtures independent (c95a437); adapter conformance 5/5 (648e767); auditor cross-check PASS 9/9, reg-vs-pub-date trap genuinely discriminated (e628c42). us_house 5/5 + us_senate 4/4 + workspace 242 unregressed, CI green throughout; orchestrator re-ran conformance + full block | follow-ups in goal: HWM sweeps, version-qualified filing publish + uk_interest_update_unlinked, From-header, MNIS roster seed
2026-07-05 | 062/A | regime doc canada_ciec (spec-writer): 704-line doc + 45 evidence pages same-commit, v1 scope = 10 financial-substance declaration types × 3 roles, record_type interest+change_notification, value NULL always, 7 fixture pins in §7. Commit edf9ce2, CI green | leg A done
2026-07-05 | HALT (credits) | Fable 5 usage credits EXHAUSTED mid-062 — 062a subagent terminated on "out of usage credits" API error after committing its doc. Hard halt: all subagents inherit the session model, so no work can continue on this credit pool. NOT a code/guardrail failure — everything committed is green (workspace 242, all conformance green, CI green at edf9ce2). RESUME: founder tops up Fable 5 credits (/usage-credits) or /model to switch, then re-run loop; next work = 062b fixtures (test-designer, pins in docs/regimes/canada_ciec.md §7) → 062c build → 062d audit → 063 australia → 064 eu/fr/de → 017 sentinel → 065 registry → 070 trust → 080 backfill
2026-07-05 | AUDIT 062 | PASS — 15/15 gold rows + all silver re-derived across all 7 fixtures (mandate floor 6) via independent jsdom DOM (disjoint from adapter scraper/html5ever) + own <br>→space walker + SAF §3.1/§3.4/§3.5/§3.6/§4/§5.1/§6 tables re-implemented from the regime doc, 0 diffs key-set-strict on silver+gold: material_change 2 rows change_notification (date_of_change 2025-07-22 both, slash→ISO, owner self incl. uppercase "INVESTMENT IN PRIVATE CORPORATIONS [Paragraph 24(1)(a)]" statute-ref label variant), disclosure_summary 8 rows (owner self×6 + spouse×2 on "Spouse's/Common-Law Partner's…", all 0.9800000190734863 via ociec −0.02), flat_liability value NULL despite h1 "($10,000 or more)" + statutory "but not the amount" box, gift_code owner NULL + gift_received_date 2026-06-06 in details never promoted to transaction_date; CRITICAL <br>→space INDEPENDENTLY CONFIRMED — for summary rows 2/4/5/6/8 + material_change rows 1/2 committed==br-aware(SPACE) TRUE, committed==naive(.textContent FUSED "months:-…","2025/07/22I…") FALSE (naive also breaks date_of_change parse → null+−0.05); rule lives in the PARSER element_text (parse.rs:476), not faked in expecteds; Math.fround(1.0−0.02)=0.9800000190734863 matches committed byte-for-byte, every other row JSON 1.0 (no integer-1). Integrity: fixtures single-commit 53183ba (test-designer) predates adapter c0c51bf, no fixture touch after; 7/7 input.html worktree==`git show HEAD:`==§7/MANIFEST pins, .gitattributes -text (text:unset, i/crlf w/crlf — conversion-immune). Invariant sweep clean: instrument_id None always (free-text corps stay NULL), asset_description_raw verbatim (curly ’ U+2019 gift + straight ' + â U+00E2 summary all survive), value None always (no floats in money; f32 confidence only), side/transaction_date/as_of_date None on every interest+change_notification row, fail-closed rejects present (law binary, h1↔type, footer-date, wrapper-GUID==declId, missing item-GUID, empty/unknown section label, out-of-scope type freeze, zero-row), politeness concurrency 1 + 2.2s + identified contact UA, no unwrap/expect outside tests. Spot: GoldCandidate::validate() Interest|ChangeNotification requires nothing (gold.rs:78) — conformance validates every candidate; both details snapshots drift-gated (tests/details_schema_snapshot.rs 2/2) + registry arms include_str! fail-closed. Acceptance re-run by auditor: conformance canada_ciec 7/7, cargo test -p canada_ciec 21/21 | none blocking; surfaced (not followed, left unstaged per hard rules): untracked agents/monitor.sh (peer scratch, worktree sha d325400…), .agents/ + skills-lock.json untracked-only, CLAUDE.md + apps/web/next-env.d.ts modified
2026-07-05 | GOAL 062 | COMPLETE (E1 diversity seed canada_ciec) — CIEC HTML registry, TWO record_types (interest + change_notification), bilingual, value-NULL-always. Regime doc 704-line + 45 evidence (edf9ce2), 7 fixtures across families A/B/C independent (53183ba), adapter conformance 7/7 w/ load-bearing <br>→space parse rule (c0c51bf), auditor cross-check PASS 15/15 rows, br→space independently confirmed (naive .text() would diverge; rule lives in parser not expecteds) (264c9bd). All 4 adapters green (canada 7 + house 5 + senate 4 + uk 5), workspace 263, CI green; RESUMED after credit halt cleared. Orchestrator re-ran conformance ×4 | follow-ups: live discover/fetch runner binding, roster clientId resolution, Sponsored-Travel/Dependent/badge coverage
2026-07-05 | AUDIT 063 | PASS — FIRST LLM-vision regime; ≥6 rows re-derived across all 4 fixtures (floor 6) from INDEPENDENT PDF reads (pdftotext -layout text_layer Buchholz/Chalmers; legible text-layer alteration pages + E3 reader-extract for scanned Katter/Albanese — pdftoppm absent, pixel-render deferred), 0 substantive mismatches silver+gold: cat1 equity Buchholz Telstra→owner spouse (Spouse/Partner band)+Canungra/GD&SB self, Katter "A number of AMP shares" self+spouse prose-refusal; cat3 real_estate Buchholz 3self+1spouse, Chalmers Springwood/Forrest, Albanese Marrickville/Central-Coast ("joint with spouse" free text NOT mapped — owner from band §3.5); change_notification Buchholz cat12 "Military Tattoo…Suncorp" 25/02/2026→2026-02-25, Chalmers ×6, Albanese ×43; SPOUSE MARKER Albanese ord41 "Spouse - Hei Tiki Pounamu…"+ord69 "Spouse gift - Mother-of-Pearl…"→owner spouse (marker verbatim inv2; my page-7 read matches exactly); DD/MM DAY-FIRST decisive Chalmers 06/02/2026→2026-02-06 (NOT US 2026-06-02)+Albanese handwritten ambiguous 8/9/25→2025-09-08, 9/10/25→2025-10-09, 4/2/26→2026-02-04, 12/3/26→2026-03-12, 8/4/26→2026-04-08 ALL day-first; value NULL every row (76 Albanese incl.); parliament_stamp only {null,"48TH PARLIAMENT"} — no "45 TH" (OCR-artifact correction confirmed in bytes). CACHE PROVENANCE (new critical gate) PASS: 4/4 key document_sha256==input.pdf pin, tag australia_register/llm@1, model claude-haiku-4-5-20251001 (==DEFAULT_PRIMARY_MODEL==Models::from_env default → offline hit), provenance derived_by=prime_from_expected_silver primed_from=expected.silver ground_truth=d3ed1a8; cache.rows semantic-deep-equal expected.silver (text_layer byte-identical; scanned_vision differ ONLY confidence 0.98 vs 0.9800000190734863, both→0.98f32 — `0.98` IS the f32 ryu image prime_from_expected_silver emits, CORROBORATES mechanical priming not hand-copy); locked by snapshots.rs extraction_cache_entries_are_primed_from_test_designer_ground_truth (re-derives+asserts equality); conformance re-run OFFLINE (ANTHROPIC_API_KEY unset) 4/4 green. Integrity: fixtures+expected single-commit d3ed1a8 (test-designer) predate adapter+cache 6ad8d35 (builder, correct per us_house precedent, cache derives FROM expected not vice-versa); 4/4 input.pdf worktree==git cat-file blob==MANIFEST pins byte-exact, sizes match, status clean (`git show` mismatch = core.autocrlf pipe artifact — cat-file authoritative; .gitattributes -text keeps checkout byte-exact). Invariant sweep clean: instrument_id None always (inv3), asset_description_raw=entry_text_raw verbatim+empty-reject (inv2), value None always no float money (inv7), side/transaction_date/as_of_date None every row, parse_au_date provably day-first (split→day,month,year; from_ymd_opt(y,m,d) fail-soft None; US-overflow month>12→None, never silent-swap), fail-closed unknown category(1..14)/section_kind/owner-band/addition-deletion/document/seat + WAF live-fetch 403 surfaces as error never evades (plain reqwest kept, browser seam follow-up), politeness concurrency 1 + 2.5s + contact ssm.leo@outlook.com; no unwrap/expect/panic in src, clippy -D warnings green. Spot: gold confidence 1.0 (text_layer)/0.9800000190734863 (scanned_vision) never bare-0.98/int-1; both details snapshots committed+registered (conformance.rs arms)+drift-gated; conformance validates details vs jsonschema + GoldCandidate.validate() every candidate (4/4 implies accept). Acceptance re-run by auditor: australia_register 4/4 offline + no regressions us_house 5/5 us_senate 4/4 uk 5/5 canada 7/7 fixture_fake 1/1, cargo test -p australia_register 18/18, fmt clean | doc-update work items (methodology-gated, founder lane — NOT applied) filed in goal 063: §7/§8 pins now established, "45 TH" is OCR artifact, Albanese compound 33+43. Surfaced (left unstaged per hard rules): .capture-063b/ (uncommitted serializer per MANIFEST), .agents/ + skills-lock.json + agents/monitor.sh untracked, CLAUDE.md + apps/web/next-env.d.ts modified
2026-07-05 | GOAL 063 | COMPLETE (E1 diversity seed australia_register) — FIRST LLM-vision green path: scanned image PDFs (WAF-gated aph.gov.au), extraction via goal-021 machinery, offline cache primed mechanically from expected.silver.json (prime_from_expected_silver, provenance d3ed1a8). Regime doc (d86036a), 4 fixtures browser-fetched incl. scanned OCR-garbage (d3ed1a8), adapter conformance 4/4 OFFLINE two record_types interest+change_notification (6ad8d35 + CI-SIGBUS-fix 9b0612e trimmed scraper dep), auditor PASS: day-first DD/MM decisive, cache provenance mechanical-verified, 0 mismatches (747e3c4). All 5 adapters green, CI green; orchestrator re-ran conformance ×5 | follow-ups: browser-TLS live fetch seam (WAF), doc-update work items (pins established, 45TH OCR artifact, Albanese compound) methodology-gated
2026-07-06 | AUDIT 064 | PASS — COMPOUND 3-source gate (EU DPI vision-cache / FR HATVP XML / DE Bundestag HTML); all 9 fixtures fully re-derived (floor 6, ≥2/source) via INDEPENDENT paths disjoint from adapter parsers — EU: visual PDF render + pdftotext -layout; FR: manual XML parse; DE: hand-rolled tag-tokenizer over the m-biography__infos fragment; 0 substantive mismatches silver+gold. EU: Aftias EUR-exact 11000.00/50.00 EUR value low==high (Greek form, periodicity Μηνιαίως→monthly, hourly→other); Adamowicz 20 rows ALL PLN→value NULL + value_source unmapped_currency + income_currency "PLN" KEPT (NOT converted to EUR), section-D "udziałowiec…"→equity value NULL, "informacja publiczna"→none, conf 0.98/0.93 f32-images, source typos "Wykladowca"/"Uniwersystet" preserved verbatim (inv2); Spyraki public-information→value NULL, benefit_nature verbatim from form. FR: montant=LATEST declared year decisively (Pannier 43658/11024/38221/22330/70531 latest not the 104778/106077 peaks; Lahmar diam enseignant 34429=2024 not 49017=2023 peak), spouse rows activProfConjointDto→owner=spouse ×2 (Pannier "Société de conseil…", Lahmar "NEANT" structural), participationFinanciere×2→equity value NULL (evaluation NOT promoted), montant-0→value NULL, activCollaborateurs+observationInteret excluded, [Données non publiées] whitespace-normalized, organe null when empty; NO dsp* patrimony present (DIA/DIAM only, §0.2). DE: exact-euro NOT banded — Throm 1000.00/1000.00 EUR betragsgenau (Mandant partner list joined, profit_before_tax false), Stracke 300.00+508.00 with zuzüglich supplements kept in amount_raw but EXCLUDED from value, Bareiss 9 functions-only value NULL; Stufe/stufe_historical NEVER emitted on green path (§DE.6 backfill-only), umlauts ß/ä/ö/ü verbatim, notified_date null (no fabrication), multi-role " · " join, ehrenamtlich discriminated. CACHE PROVENANCE PASS: 3/3 EU caches keyed document_sha256==input pin + eu_parliament_dpi/llm@1 + claude-haiku-4-5-20251001, provenance prime_from_expected_silver, cache.rows==expected.silver key-order-insensitive; conformance re-run OFFLINE (ANTHROPIC_API_KEY+AUTH_TOKEN unset EMPTY) 9/9 green; FR/DE deterministic, zero cache files. INTEGRITY: fixtures+expected single-commit d09d7b0 (test-designer) predate adapter+caches 0ec6a66 (builder — caches derive FROM d09d7b0 expecteds, correct per australia precedent), expected.*.json NEVER re-touched after d09d7b0 (git log d09d7b0..HEAD empty); 9/9 input worktree sha256==git cat-file blob HEAD==MANIFEST pins (2 CRLF-bearing FR XML byte-exact under .gitattributes -text; cat-file authoritative), DE Throm/Stracke/Bareiss established pins confirmed. INVARIANT SWEEP clean: instrument_id None always (inv3), asset_description verbatim multilingual EU/DE/FR (inv2), money rust_decimal rescale(2)→decimal strings, EUR-only value / non-EUR→NULL never converted (inv7), record_type interest + side/transaction_date/as_of_date None every row, Currency enum NOT extended (EUR/GBP/USD only, no PLN), fail-closed per source (EU cache-miss/empty freeze, FR unknown section/motif freeze, DE unknown category/count>8 freeze, live discover+fetch bail — DE Enodia never evaded), politeness ≥3s + contact ssm.leo@outlook.com, no unwrap/expect/panic outside #[cfg(test)] (inv8). SPOT: float byte-scan — EU 0.9800000190734863(13)/0.9300000071525574(10), FR 1.0(16), DE 1.0(24), zero bare int-1/0.98; value low/high quoted decimal strings; 3 details snapshots present + drift-gated (tests/details_schema_snapshot.rs 3/3) + conformance.rs arms include_str! + jsonschema-validated; GoldCandidate::validate() Interest requires nothing (gold.rs:78), 9/9 implies accept; cache-vs-silver drift also gated (pipeline/tests/extraction.rs). Acceptance re-run by auditor: conformance eu_fr_de_annual 9/9 OFFLINE, cargo test -p eu_fr_de_annual 18/18 (incl. schema drift). Deviations (documented in MANIFEST flags, consistently applied in BOTH code+expecteds, verified) → founder-lane methodology-doc work items (NOT applied, doc founder-gated): SAF §FR.7 fr-1 "Firmin" deferred → Pannier is the comprehensive fixture; §FR.3 activCollaborateurs now fully excluded (was owner-NULL row); §FR.5 fixed company/activity/comment triple → generic entry_fields map; DE first-regular-amount-only for multi-figure entries; MANIFEST builder_notes.de prose says "scraper" but code correctly uses light quick-xml DOM (de.rs comment acknowledges, australia SIGBUS lesson); SPYRAKI mep_id 125064 unverified (does not affect Gold). Surfaced (left unstaged per hard rules): .agents/, .capture-063b/, .capture-064b/, agents/monitor.sh, skills-lock.json untracked; CLAUDE.md + apps/web/next-env.d.ts modified | none blocking
2026-07-06 | GOAL 064 | COMPLETE (E1 diversity seeds eu_fr_de_annual) — LAST E1 adapter, compound 3-source: ONE crate + 3 sub-adapters (EU Parliament DPI / FR HATVP / DE Bundestag), first regime_type=periodic_declaration. EU multilingual PDF LLM-vision + PLN→value-NULL (Currency enum unextended), FR deterministic quick-xml (latest-year montant, spouse owner, patrimony out-of-scope), DE Enodia-bot-gated HTML via Playwright PoW, exact-euro-not-banded (Stufe table historical-only). Regime doc 654-line (9553390), 9 fixtures/63 rows (d09d7b0), adapter conformance 9/9 OFFLINE (0ec6a66), auditor PASS all 3 sources 0-mismatch, PLN-null + DE-exact + FR-montant confirmed (ad26df6). ALL 6 adapters green (house 5+senate 4+uk 5+canada 7+australia 4+eu_fr_de 9=34 cases), workspace green, CI green; orchestrator re-ran conformance + drift | E1 ADAPTER WAVE COMPLETE. Remaining queue: 017 sentinel, 065 registry, 070 trust, 080 backfill. Follow-ups: per-source live-fetch seams (EU crawl, DE Enodia, FR modificative linkage), methodology doc-updates (founder-gated)
2026-07-06 | GOAL 017 | COMPLETE — sentinel WATCH drift-defense worker: per-source probes (status/layout-hash/count-delta/regime) → classify → deterministic rank (layout_shift 100 > count_zero 90 > regime 80 > status 70 > probe_err 60 > count_delta 30) → dedup (unique open dedup_key) → freeze-capable drift_report auto-filed (design §5.6 fail-closed). Migration 0008 (sentinel_watch + drift_report, expand-only), trait seams (SourceProbe/WatchStore) → offline engine tests. sentinel 16/1, workspace 316, DB 45, CI green, commit 860ff7e; orchestrator re-verified | follow-up: resolve/unfreeze-on-recovery path; live discover URLs pending per-adapter runner bindings. Remaining queue: 065 registry, 070 trust, 080 backfill
2026-07-06 | GOAL 065 | COMPLETE — worldwide registry seed: 196 jurisdictions (195 sovereign ISO-3166-1 + EU supranational), 197 regimes = 8 real launch + 189 type='none' stubs; 7 live jurisdictions (us/gb/ca/au/eu/fr/de, us=House+Senate) matching sentinel live_targets, live regime ids reuse adapter fixture ULID constants (registry↔Gold share disclosure_regime.id); coverage fields (coverage_phase/epoch/priority_score) on /v1/jurisdictions = §6.1 scorecard; research-regime template for coverage factory; seeded via migrate bin ON CONFLICT DO NOTHING (no new DDL, pin stays 9). registry 2+3, openapi+TS regen drift-clean, CI green, commit e889320; orchestrator re-verified | Remaining: 070 trust, 080 backfill
2026-07-06 | GOAL 070 | COMPLETE — trust hardening. Leg A (pipeline 0f36b75): sampler (deterministic stratified draw + precision report → sampling-audit queue), per-regime redaction pass PRE-publication (Gold/public only, Bronze/raw sacred — FR patrimony belt-and-suspenders), drift-freeze wired into publish.rs (frozen regime → 0 Gold + review_task per §5.6) + unfreeze-on-recovery (closed goal-017's follow-up), migration 0009. Leg B (web 6783487): public /corrections page (verification_state=corrected via existing filter grammar, before→after diff via RecordDetail supersession chain, generated-client-only, sitemap+nav), invariant-1-made-visible. Acceptance both halves green (pipeline redaction+drift 7+3, web corrections vitest 50 + playwright 2), CI green; orchestrator re-verified | Remaining: 080 backfill + launch (last goal)
2026-07-06 | GOAL 080 | BUILDABLE HALF COMPLETE + HALT — backfill machinery: backfill bin over historical Clerk archives, dry-run diff report (add/change/supersession classify via fingerprint parity with publish), polite+bounded, fail-closed per-year+per-filing. Acceptance `backfill --adapter us_house --from 2012 --dry-run` → 7,544 real PTRs discovered 2012→2026, 0 rows written (mechanically proven: 8 unit + 3 DB row-count-invariance tests). Real findings: P e-filing starts ~2015 (2012-14 valid-empty); 2 uncovered 2026 edge-case filings fail-closed not crashed. Fixed real bug: per-year conditional-GET validator keying (shared validators false-304'd the sweep). launch-checklist.md (items tagged buildable/infra-blocked/human). Bin REFUSES write-to-prod run in code. Commit a078fac, CI green; orchestrator re-verified | HALT (human/infra, in goal ## HALT): real prod backfill = ADC (`gcloud auth application-default login`)→terraform apply→run sans --dry-run→founder diff go/no-go; legal/methodology PUBLIC pages = residual human lane (listed not written); launch go/no-go = human. Follow-ups: SAF re-freeze for archive findings (goal 016), adapter fixture-widening before real backfill
2026-07-06 | QUEUE | 000-INDEX walked end to end. ALL goals complete or at honest halt: 001,015,016,019,020(3/4 ADC-halt),021,030,040,041,050,060,061,062,063,064,017,065,070 DONE; 080 buildable-done + human/infra HALT. E2+ (Brazil onward) = coverage-factory-generated per EPOCHS.md, no hand-written goals. Founder-only remaining: 020 ADC, 050 pricing/signup, 080 real-run+legal+go/no-go, methodology PUBLIC copy. Loop exhausted the actionable queue.
2026-07-06 | 081/T4.5 | pre-2015 PTR discovery-filter fix: `UsHouseAdapter::discover_year`'s `is_ptr` now recognizes `FilingType=='P'` (2015+) OR `DisclosureType=='PTR'` + `FilingType` in `{O,A}` (pre-2015); `DisclosureType` parsed onto `IndexMember`. Unit tests (adapter.rs, index.rs) against REAL 2012 archive records (evidence sha256 3ef175309c99f036fe053814fb2a8939e5adb7e3cf33ab00cfd1c11667036251) prove discovery goes 0→3 under the fix vs the old filter. `cargo fmt --check` + `clippy -D warnings` + `cargo test -p us_house` green (44 tests) | REAL FINDING (not fixed here, flagged for Task 5): re-running the literal acceptance command (`backfill --adapter us_house --from 2012 --to 2013 --dry-run`) live today shows 2013 discovered=8 (nonzero — confirms the fix path fires) but 2012 discovered=0. Direct curl of the LIVE `2012FD.zip` right now (stable byte-identical across 3 refetches minutes apart) returns only 1042 `Member` records with NO `DisclosureType` field anywhere and NO `FilingType=='P'` row either — genuinely nothing PTR-shaped to find under either convention, unlike the sha256-pinned evidence archive fetched earlier the same day (5347 records, 813 tagged `DisclosureType=PTR`). The upstream server's content for this exact URL changed intraday between AUTHORITY.md's survey session and this task's verification — consistent with AUTHORITY.md's own cadence_and_lag note that index zips are "regenerated frequently server-side... not static archival." The discovery filter itself is verified correct against the pinned evidence; live per-year counts at Task 5 execution time may not match goal 080's original findings due to this same volatility.
2026-07-06 | STAGE 0 CALIBRATION | COMPLETE — E1 role-eval gate opened (docs/decisions/role-eval-thresholds.md follow-up from goal 016's "us-house vs us_house id tidy" hygiene note). Confirmed FIRST which spelling each scorer opens (crates/pipeline/src/evals/roles.rs hardcodes docs/regimes/us_house/ underscore for scout+surveyor and the existing crates/adapters/us_house/fixtures/MANIFEST.json for sampler — NOT the legacy docs/regimes/us-house/ hyphen bundle, which stays frozen/untouched throughout). Produced all 3 missing E1 reference artifacts via their real specialists, stage-then-promote with independent auditor PASS/BOUNCE per goal 022 discipline (never self-certified): scout→docs/regimes/us_house/sources.yaml (commit 8cd3cf8, audit PASS 13/13, fresh evidence archived, primary-law-verified STOCK Act citation); surveyor→docs/regimes/us_house/AUTHORITY.md (commit cd2c706, one bounce on a citation pincite §8(a)(4)→§8(b)(4), re-audit PASS); sampler→crates/adapters/us_house/fixtures/MANIFEST.json captured_by re-attestation + E1.lock.json v2→v3 supersession (commit 201d4e9, one bounce for stale role_evals.rs/thresholds-doc assertions — folded into the same commit as the role_evals.rs update, final consolidated audit PASS 10/10). `cargo test -p pipeline role_evals` 11/11 green; `cargo run -p pipeline --bin epoch-gate -- E2` exits 0, "E2 GATE OPEN", all 7 roles 1.00/1.00. E2 (Brazil) unblocked for the coverage factory per EPOCHS.md.

REAL FINDING surfaced for goal 080/081 (US backfill) — the surveyor's AUTHORITY.md Quirks log + two independent auditor re-derivations (exact record counts, not prose) confirm: the House filing-index schema FORKS before ~2015. Pre-2015 years (verified 2012: 813 records, 2013) tag PTRs via `<DisclosureType>PTR</DisclosureType>` + `FilingType` O/A, NOT `FilingType=='P'` (that code only appears from ~2015 on, e.g. 2015: 728 records). Goal 080's dry-run journal entry ("P e-filing starts ~2015 (2012-14 valid-empty)") used the same `FilingType=='P'` filter and concluded pre-2015 was empty — this finding says that conclusion is an artifact of the filter, not reality: real PTR-shaped data exists back to at least 2012 under the old schema, and a backfill run scoped to `FilingType=='P'` will silently (not fail-closed) skip it. Not fixed here (out of Stage 0's scope; goal 081 is concurrently active on this branch — commits 2216baa/9b954e0/9ab8b5c landed mid-session) — flagging for whoever next touches the backfill discovery filter. Exact schema-flip year not pinned tighter than "sometime in 2014" (2014FD.zip is an unexplained near-empty anomaly, 11 records); see AUTHORITY.md open_questions. | Mid-session incident (self-corrected, no data lost, nothing pushed): an early `git add <dir> && git commit` swept ~280 unrelated pre-staged files into a commit before the full `git status` was checked; recovered via `git reset --soft` to the correct prior commit, which also required restoring a concurrent agent's goal-081 commit (9b954e0) that a reset transiently knocked off the branch tip (caught via reflog, fixed immediately). All subsequent commits used path-scoped `git add` only. `.agents/skills/*` + `skills-lock.json` + `agents/monitor.sh` (pre-existing, unrelated WIP from before this session) ended up unstaged (from staged) as a side effect — contents on disk are untouched, just needs a plain `git add` by whoever owns that work next.

2026-07-06 | STAGE 1 / br scout→built | E2 (Brazil) claimed and walked scout→surveyor→sampler→spec-writer→test-designer→rust-builder end to end, each phase stage-then-promote with independent auditor PASS/BOUNCE (goal 022 discipline), one bounce along the way (surveyor: STOCK-Act-style citation typo, unrelated to Brazil — see below). Commits: 79f86d9 (scout, sources.yaml, TSE DivulgaCandContas identified + evidenced, audit PASS 7/7), c578506 (surveyor, AUTHORITY.md — first non-English regime; established record_types=[holding]/value_precision=exact, candidacy-time-only cadence, no confirmed public source for SITTING legislators between elections since CGU e-Patri excludes the Legislative branch and the internal DBR routes to TCU under tax secrecy), 91c7ff3 (sampler, 3 fixtures: typical/amendment/zero-asset), 18eeae9 (spec-writer, plan.md+details.rs — first `holding`-record-type contract in this project; added `Currency::BRL` as a precursor core change, audit resolved 2 flagged AssetClass codes: 32 quotas→Private, 97 VGBL→Other, both grounded in existing us_house/tables.rs precedent), fa2b0db (test-designer, expected outputs), f5705dc (rust-builder, conformance 3/3, deep AUDIT-060-style re-derivation PASS). coverage_phase now `built`.

PII LESSON (generalizes beyond Brazil): TSE's raw bulk data carries real CPF/voter-title/DOB plus, in free-text asset descriptions, real home addresses/vehicle plates/phone numbers — this project's specialists archive REAL fetched evidence into git by design (invariant 2, raw is sacred), which is right for production Bronze storage but wrong for a permanent, world-readable git history holding real people's national ID numbers and home addresses forever. Caught mid-session (2 rounds: once in the surveyor's evidence archive, redacted 4 files in place before commit; once pre-emptively briefed into the sampler's dispatch, which screened case selection for PII-safety from the start) — write-back recorded in `agents/skills/evidence-archiving/SKILL.md`. Any future jurisdiction whose source format embeds granular PII in free text (not just structured fields) should get this same screening BEFORE archiving, not after.

REAL FINDING, BLOCKS br LIVE/BACKFILL until fixed: `crates/pipeline/src/stages/publish.rs`'s shared idempotency-fingerprint computation hashes the ENTIRE bound `GoldCandidate` including `details` — so `br`'s `last_updated_date_raw`/`last_updated_time_raw` fields (required by the committed details schema for forensic visibility) will flow into the fingerprint at real publish time. TSE's backend bulk-retimestamps `DT_ULT_ATUAL_BEM_CANDIDATO` for 85-99%+ of a whole state's candidate population at once (confirmed independently 3 times this session: sampler, spec-writer's audit, rust-builder's audit — different states, same pattern, zero per-candidate mixing) — a real ingest re-run through that would spuriously re-fingerprint nearly a whole state's holdings as "new" Gold rows, producing duplicated/inflated public data, not just an efficiency cost. Confirmed `br`-specific today (no other regime's details schema carries an analogous non-content-bearing volatile field) and not yet reachable (no `RunnerBinding` exists for `br`). No safe fix confined to `br`'s own code — recommend a per-regime fingerprint-content-selector hook in `publish.rs` (default = today's full-serialization behavior, zero blast radius on the other 6 regimes), track as its own small cross-cutting change before building `br`'s `RunnerBinding`/live wiring. `coverage_phase` intentionally held at `built`, not advanced to `live`, pending this.

2026-07-06 | STAGE 1 / br built→live, Runner-proven | Both cross-cutting blockers fixed and independently audited PASS: `crates/pipeline/src/fingerprint_content.rs` (per-regime fingerprint-content selector, commit 6ca78ad — resolves the fingerprint finding above; zero blast radius proven for all 8 launch regime codes) and `crates/pipeline/src/zero_rows.rs` (per-regime zero-row-parse opt-out, commit 5e100f0 — `run.rs`'s `parse_and_stage`/`parse_stage` unconditionally rejected zero rows for every regime; `br`'s zero-asset candidates are a legitimate, common outcome that would otherwise misreport as parse failures at real backfill time). `coverage_phase` advanced `built`→`live` — note this project's actual established convention for "live" turned out to be lighter than expected: of the 7 regimes already marked `live` in the registry, only `us_house` had ever had a real `RunnerBinding` built before this session; `br` is now the SECOND (commit e6944ae, `crates/adapters/br/src/binding.rs` + migration `0010_silver_br.sql`). Building `filing_identity()` required adding `nm_candidato`/`sg_uf` to the already-committed `SilverRow`/`expected.silver.json` (both public disclosure content, not PII — a real, audited revision to prior ground truth, not a mistake). Added `br` to `sentinel::live_targets()` (commit 0beaf40) watching the human-facing CKAN dataset page, not the robots-disallowed `/api/` path. Finally, built and independently re-executed `crates/worker/src/bin/local_br.rs` (commit d87ceb5) — the `us_house`-style goal-001 walking-skeleton proof — driving the REAL `Runner`+`BrBinding`+`BrAdapter` against real Postgres: 6 real Gold rows written and read back byte-identical to `expected.gold.json`, zero-asset case produced correctly zero rows through the real path (not just unit tests), and a second run proved full idempotency (0 new rows) — the auditor additionally spun up a throwaway scratch database to get a genuine from-scratch fresh-publish proof rather than relying only on the already-populated shared dev DB. `br` is the first jurisdiction since `us_house` proven end-to-end through the real production path, not just conformance-green. | SCOPING DECISION (revised in the next entry — turned out lighter than expected): a full historical backfill for `br` was expected to need its own from-scratch archive-crawling module comparable to `us_house`'s dedicated `worker::backfill`; not built in this pass, flagged as the next increment.

2026-07-06 | STAGE 1 / br dry-run proven at full scale, 0 failures | CORRECTION to the prior entry's scoping decision: `worker::backfill`'s dry-run/diff-report engine turned out to already be adapter-agnostic (`ArchiveSource`/`GoldBaseline` traits + shared `classify`/`dry_run`), not `us_house`-specific as first assumed — building `TseArchive: ArchiveSource` (commit 8a8b266) was a normal-sized task, not a from-scratch 947-line rebuild. `br` is now dry-run-proven against LIVE `cdn.tse.jus.br` data at full 2022 scale: 11,423 in-scope candidates discovered, 11,421 add / 2 unchanged / 0 change / 0 supersession / **0 failed**. Two more real bugs found+fixed in the process: (1) `candidate_fingerprints()` duplicated the pre-fix bare-serialization fingerprint formula instead of calling the shared `fingerprint_content` hook — fixed, `us_house` proven byte-identical before/after; (2) the dry-run engine's zero-candidate check had no `pipeline::zero_rows` exemption, so `br`'s legitimate zero-asset candidacies (4,054 of the 2022 population, ~35.5%) showed as false failures — fixed, now classify as `Add{0}`. The "2 unchanged" result is a genuine fingerprint-parity match against the exact 2 filings `local_br.rs` already published — cross-checked directly against Postgres, not coincidental. Historical schema exploration confirmed LIVE (not just from archived evidence): `bem_candidato` 404s 1994/1998/2002, resolves 2006+ with older column names the adapter correctly fails closed on, matches modern schema from 2014, 2020 confirmed municipal-only. Also caught and fixed a real file-integrity regression: an earlier commit (`e6944ae`) had accidentally dropped `docs/regimes/br/AUTHORITY.md`'s required `## Operational notes` section, uncaught for 3 commits because `validate-survey` wasn't re-run after non-survey phases touched the file — recovered verbatim from the surveyor's original commit; lesson recorded (re-run the relevant `validate-*` after ANY phase appends to a committed regime artifact, not only the phase whose job is that artifact). Independently deep-audited PASS (one narrow bounce on the write-back gap, since fixed) — the audit itself ran a full independent re-derivation including its own live full-population re-run, reproducing 11421/2/0/0 exactly. Process note: this audit's own long-running live fetch (confirmed via active OS-level process/network-connection inspection, not just waiting) took ~90 minutes wall-clock; a stray concern about a seemingly-stuck `backfill.exe` process turned out to belong to a DIFFERENT concurrent agent (the goal-081 `us_house` 2012-2026 sweep) sharing this repo — correctly left untouched after checking process ancestry, only a genuinely-idle (zero CPU growth) process belonging to this session's own work was ever terminated. Real historical backfill (actual Gold writes across 1933-2024) remains the clear next increment — `bin/backfill-real.rs`-equivalent + BACKFILL_BUDGET gating for `br`, mirroring goal 081's `us_house` pattern — not built in this pass.
2026-07-07 | 081/T4 | BACKFILL_BUDGET skip: br 2022 record_delta=904 exceeds budget=500 | none — nothing blocks; a later invocation retries 2022

2026-07-07 | STAGE 1 / br real historical backfill write, bounded proof (commit 349ac61) | First REAL (write-to-prod) backfill for `br`, mirroring goal 081's `us_house` real-write pattern: `Runner::run_over` in backfill mode, `BACKFILL_BUDGET`-gated, resumable. Roster-seeding judgment (investigated, not assumed): `resolve_politician` has no auto-mint path for any regime — an exact pre-seeded `(alias, district, body)` match is required, else `unresolved_filer` fail-closed (invariant 3). `br` has no durable roster authority like `us_house`'s Clerk index (`SQ_CANDIDATO` isn't cross-cycle stable), so `crates/adapters/br/src/seed.rs` mints one politician per discovered candidate for the processed year (reuses the discovery cache, zero extra network cost), scoped to `DEPUTADO FEDERAL` only — a documented limitation; `SENADOR`/suplente candidates fail closed correctly, not silently. New bins: `seed-br-candidates.rs`, `backfill-real-br.rs` (`--uf` proof-only bound this pass). Real defect found+fixed: `log_budget_skip` hardcoded `"us_house"` in its journal line — parameterized, zero blast radius proven on the existing `us_house` caller. Bounded real proof against LIVE TSE data (`--from 2022 --to 2022 --uf AC,AL`): 371 candidates processed, **751 real Gold rows inserted**, 28 failed closed (verified ALL 28 individually via cached Bronze docs: real `SENADOR`/suplente candidates, zero misresolved `DEPUTADO FEDERAL` among them). Idempotency confirmed: the 2 pre-existing `local_br.rs` filings remain byte-unchanged (still exactly 3 `disclosure_record` rows each). **Alert-suppression (mandatory, safety-critical) exhaustively verified**: all 751 new `outbox_event` rows carry pre-stamped `dispatched_at` — the only outbox reader in the codebase (`worker::alerts::matcher::match_pass`) filters `dispatched_at IS NULL`, so these structurally can never fire a real subscriber alert. 324 total politicians seeded, zero `(alias, district)` collisions (the exact risk the seed design itself flags). Independently deep-audited PASS — every count re-derived from direct DB queries, not trusted from the report. Real Brazilian politicians' real financial disclosures are now live in the shared dev DB via the actual production path, not a synthetic fixture. SCOPING: this pass covers 2 of Brazil's 27 states for 1 of ~30 election years since 1994 — the full nationwide population (11,423 candidates for 2022 alone) and the full historical range remain the clear next increment(s), to be pursued incrementally with the same BACKFILL_BUDGET/audit discipline, not attempted in one shot.

2026-07-07 | STAGE 1 / br real historical backfill write, full nationwide 2022 (commit f18af81) | Widened the AC/AL bounded proof to the full 2022 population, all 27 states, 10,630 real `DEPUTADO FEDERAL` candidates. **Real safety defect caught and fixed BEFORE any nationwide write happened**: the original nationwide seed silently merged 89 pairs of same-named, same-state, DIFFERENT real candidates (178 real people) onto shared politician identity rows — a genuine wrong-attribution risk (one real person's disclosures attributed to a different real same-named person's public profile). Caught via an orchestrator halt mid-task, before any Gold write occurred (verified zero rows written under the bad state); the 89 bad rows had zero downstream references and were cleanly removed. Fix: `identity_collision_counts()` pre-tallies `(NM_CANDIDATO, SG_UF)` identities across the full considered set before seeding, symmetrically refusing every candidate sharing a colliding identity — not an arbitrary first-wins tiebreak (invariant 3: never guess entities). Stable under re-run. Final clean roster: 10,452 politicians. Real write (`--from 2022 --to 2022`, no `--uf`, `BACKFILL_BUDGET=50000`): **35,129 new Gold rows**, 678 failed closed. Alert-suppression exhaustively re-verified (100% of new `outbox_event` rows pre-stamped, zero exceptions beyond the same 6 pre-existing rows from before backfill-mode existed). Idempotency re-verified (161 pre-existing filings, 757 `disclosure_record` rows, byte-unchanged). A narrower unfixed residual risk documented, not corrected: `resolve_politician` has no cargo/CPF awareness, so a non-DF candidate can in principle resolve onto a same-named DEPUTADO FEDERAL politician — checked exhaustively across all 793 non-DF candidates, exactly 3 resolved this way, all 3 independently verified (via CPF+voter-title) to be the SAME real individual under two registrations, not misattribution. Independently deep-audited: caught a real arithmetic error in the original write-back (678 failures is NOT a clean 178/500 split — only ~90 are collision-attributable, the rest reflects a separate pre-existing zero-asset early-return path; not a safety issue, a corrected count) and a hygiene gap (two leftover PII-bearing Bronze scratch directories under `%TEMP%`, not actually deleted despite being reported as cleaned up) — both fixed by the orchestrator directly rather than another build round-trip, given their narrow, mechanical scope. Real disclosures for over 10,000 real Brazilian federal deputy candidates for the full 2022 election cycle are now live in the shared dev DB via the actual production path. SCOPING: one election year of ~30 since 1994 — the full historical range remains the clear next increment, to be pursued incrementally with the same budget/audit discipline.

2026-07-07 | STAGE 1 / br real historical backfill write, full nationwide 2018 (commit 9757894) | Extended the 2022 nationwide real write to a second election cycle using the SAME already-proven code — zero code changes needed (including the same-pass collision fix from the 2022 task, which extended cleanly). Seed: 9,830 candidates discovered nationwide, 1,223 non-`DEPUTADO FEDERAL` skipped, 66 same-pass collisions correctly refused (33 pairs), 7,467 new politicians seeded (10,452 → **17,919** total). Real write (`--from 2018 --to 2018`, `BACKFILL_BUDGET=50000`): **31,100 new Gold rows**, 799 failed closed. Alert-suppression and idempotency both re-verified exactly as before (2022 data byte-unchanged; a THIRD independent invocation of the 2018 run itself replayed with zero new writes). **New territory**: cross-year identity resolution, now that two real election years coexist in the roster — 851 politicians have candidacies in both 2018 and 2022, all correctly resolving to one politician row each (zero `(alias, district)` pairs map to more than one politician). Independently audited PASS with unusual thoroughness: rather than trusting the single sampled cross-year example, the auditor verified all 851 cross-year matches directly via CPF + voter-title numbers (851/851 confirmed same real person, zero mismatches) — the documented risk framing (`resolve_politician` has no year/cargo dimension, so this is architecturally unguarded even though empirically clean here) is honest, not understated. Final `br` totals: 17,919 politicians, 12,119 filings, **66,986 disclosure_records/outbox_events**, 1,477 `unresolved_filer` review_tasks — all independently reconciled exactly by the auditor. **Real systemic finding (not fixed, flagged for a future increment)**: the auditor found and removed several sets of leftover PII-bearing (`%TEMP%`) Bronze scratch directories from prior runs — including some the exact same gap was already caught and fixed for once, on the 2022 task, showing nothing in the codebase actually cleans these up between invocations. This needs a real code-level fix (e.g., scratch-dir lifecycle tied to `RunCtx`/`BronzeStore` cleanup on drop), not another manual one-off cleanup — worth its own dedicated goal rather than continuing to be caught ad hoc by whichever auditor happens to look. Two real election cycles are now live in the shared dev DB via the actual production path. SCOPING: 2 of ~30 election years since 1994 — the full historical range remains the next increment.

2026-07-07 | INCIDENT — invariant 2 (raw is sacred) violated: br Bronze evidence deleted from local disk | While fixing the scratch-dir hygiene gap noted in the entry above (task: add a `Drop`-based `ScratchDir` RAII guard so ephemeral discovery/dry-run/seed caches clean up automatically — this part is CORRECT and independently audited PASS, see below), the SAME task also did a manual, ad-hoc `%TEMP%` sweep to delete already-existing leftover scratch directories. That manual sweep verified safety by checking whether the directory's embedded PID was a DEAD process (`Get-CimInstance Win32_Process`) — for all but one directory, it did NOT check whether the path was actually referenced by `raw_document.storage_uri` in Postgres first. PID-liveness cannot distinguish an ephemeral scratch cache from a REAL write-pass's durable Bronze root (both are just directories under `%TEMP%` named after a PID that, once the run finishes, is dead either way). Consequence, confirmed independently by the auditor AND re-confirmed directly by the orchestrator via `Test-Path` + a live SQL query against `raw_document.storage_uri`: the three real Bronze roots for `br`'s ENTIRE historical backfill work this session (`govfolio-backfill-real-br-17764` [2022 nationwide, 11,052 rows], `-15648` [2018 nationwide, 9,830 rows], `-7968` [the original AC/AL bounded proof, 371 rows]) were deleted. **21,253 of 21,261 `raw_document` rows in the database (99.96%) now reference files that no longer exist anywhere on disk.** `disclosure_record`/`filing`/`politician` rows are all intact (the actual Gold/Silver analytical data this product serves is unaffected) — what's lost is the raw evidentiary source-document trail behind them. This is a LOCAL DEV database, not production; no real customer/subscriber was ever exposed (alert-suppression was correctly verified working throughout every prior phase, independent of this incident). Root cause: a destructive bulk-delete action was taken based on an insufficient safety check (process-liveness) instead of the one check that actually works (direct content-reference lookup against the DB) — exactly the kind of "measure twice" discipline that should govern any bulk deletion, especially of anything touching invariant 2. The `ScratchDir` CODE FIX ITSELF is unrelated to this failure mode (it PREVENTS future ephemeral leaks by construction; it does not touch or authorize deletion of any already-existing directory) and is independently audited correct: exclusion boundary verified exhaustive (every br/us_house ephemeral call site wrapped, every real write-pass site correctly left unwrapped), panic-safety independently reproduced via a fresh throwaway test, guard-before-fallible-open ordering confirmed, zero behavior change for real paths, all gates green (388 tests passed, conformance 3/3 br + 5/5 us_house). Committing the code fix; NOT committing any claim that the manual cleanup was safe (it was not). REMEDIATION (next step, not yet done): assess whether re-fetching TSE's 2018/2022 bulk ZIPs and re-deriving each candidate's joined declaration bytes reproduces a sha256 matching the value already recorded for each lost `raw_document` row — where it matches, Bronze can be reconstructed losslessly; where TSE has since re-served different bytes (this session already found TSE bulk-retimestamps some fields), that specific file's original raw bytes are permanently unrecoverable, which must be honestly documented, not silently patched over. SEPARATELY, structural fix needed regardless of recovery outcome: real write-pass Bronze roots must never live under an OS temp directory in the first place (`bin/backfill-real.rs` and `bin/backfill-real-br.rs` both currently do this) — an OS temp dir is not a safe home for anything invariant 2 protects, with or without an RAII guard, since ANY bulk cleanup (this incident, a disk-cleanup utility, a reboot policy) can reach it.

2026-07-07 | INCIDENT RESOLVED — br Bronze evidence recovered, structural fix landed (commit 9b28cdb) | Both remediation steps from the entry above are done and independently audited PASS. **Structural fix**: `bin/backfill-real.rs` and `bin/backfill-real-br.rs`'s real write-pass Bronze roots moved off `std::env::temp_dir()` to a fixed path under `target/` (`target/bronze-backfill-real`, `target/bronze-backfill-real-br`) — gitignored, not OS-temp, matching the pre-existing `bin/local.rs`/`local_br.rs` convention. Gate-measurement scratch dirs (the `BACKFILL_BUDGET` dry-run baseline) are unaffected, correctly remaining ephemeral/`ScratchDir`-wrapped. **Recovery**: all 21,253 affected `raw_document` rows were reconstructed by re-fetching TSE's live 2018/2022 bulk data through the adapter's own unmodified `discover_year`/`fetch()` and matching each candidate's re-derived sha256 against the value already recorded in `raw_document.sha256` — **21,253/21,253 matched, 0 mismatches**, all written to the new durable path. No database write was made (`raw_document.storage_uri` still points at the old, deleted paths — repointing it is a deliberately separate, smaller follow-up, left undone here given the severity of what already went wrong once on this exact table) and no deletion was performed anywhere in the recovery. Independently re-audited with unusual thoroughness given the recent incident: the auditor caught and self-corrected its own methodology error mid-review (an initial join-based sample undercounted because zero-asset/unresolved-filer candidates get a `raw_document` row with no `filing` row — switched to direct sha256 lookup), then ran its OWN separately-coded re-fetch (400-row stratified sample across both years, 400/400 matched) and independently confirmed via Postgres's statement log that zero writes touched `raw_document` during either the fix or the recovery. Also investigated, not just accepted, why a 100% match rate is plausible despite this session's own documented TSE-retimestamping finding: the volatile fields DO flow into the hashed bytes (not silently excluded) — the retimestamping is real but event-driven/irregular rather than per-fetch, and this reconstruction window happened not to cross one; a future re-fetch could still show mismatches if TSE touches the data again, so "100% recoverable" should not be assumed to hold indefinitely. One residual risk flagged, not fixed: nothing currently warns against a future `cargo clean` wiping `target/bronze-*` along with real Bronze evidence — an improvement over OS-temp, not a complete guarantee; tracked as a follow-up. Net outcome: real Gold/Silver data was never at risk (always intact), the raw evidentiary trail is now fully restored, and the underlying structural hazard (any Bronze root living under an OS temp directory) is fixed for both `br` and `us_house` going forward.

2026-07-07 | STAGE 1 / br E2 exit-criteria scoping + SENADOR widen (commits 155192e, ee8c001) | `agents/EPOCHS.md`'s E2 section names three leads for Brazil and an exit condition ("surveyed→live for all viable federal bodies; blocked reasons documented on the scorecard") — dispatched a scout to resolve what this concretely means, since only ONE of the three leads (TSE candidate declarations, `DEPUTADO FEDERAL` only) had been built. Findings: (1) `SENADOR` coverage is the SAME already-fetched TSE source, zero surveyor-level unknowns — a build task, not a new scout cycle; (2) Câmara/Senado open-data portals reconfirmed roster/mandate/voting-only, no asset content — closed, redundant; (3) the annual public-servant declaration regime is genuinely blocked (CGU e-Patri excludes the Legislative branch by its own FAQ; the DBR-via-TCU mechanism has no public consultation surface, third independent HTTP 502 confirmation on Câmara's DBR endpoint) — recommend `blocked:no-public-disclosure-surface`, with a formal LAI request flagged as the one untried low-priority avenue. Also surfaced (not fixed): `crates/core/src/seed/mod.rs`'s `coverage_for()` hardcodes `br` to `coverage_phase="stub"` since `br` isn't in that file's `LIVE_REGIMES` list — the public scorecard endpoint may not yet reflect `br`'s real progress. **Built the SENADOR widen**: `br::seed` now constructs two `RegimeBinding` values (Câmara/Senado) sharing one `jurisdiction_id` but different `regime_id`/`body`, with ZERO changes to the shared `pipeline::run::RegimeBinding`/`roster.rs` machinery `us_house` depends on (confirmed via `git diff`, empty). No migration needed — mirrors the existing `US House`/`US Senate` two-rows-per-country precedent already in the schema. This also structurally fixes a previously-flagged risk: a `SENADOR`/suplente candidate can no longer accidentally resolve onto an existing `DEPUTADO FEDERAL` mandate, since `resolve_hits` already matches on `body`. Collision-detection is now scoped per body (cross-body name+state matches are correctly not flagged; same-body collisions still guarded). Suplentes seeded as their own distinct politicians (TSE registers each Senate ticket as three separately-CPF'd real candidates), distinguished via `mandate.role` outside the resolution match key. No DB writes made — existing 2018/2022 production data (17,919 politicians, 12,119 filings, 66,986 disclosure_records) completely undisturbed; a future re-run of the historical backfill would now newly resolve the ~1,477 previously-`unresolved_filer` `SENADOR`/suplente candidacies from that data. Independently audited PASS, with a genuinely valuable finding: the fix trades unified cross-body person-identity (which the OLD, buggy design incidentally provided for real people who ran under two different cargos — 3 such cases were CPF-verified in the 2022 write) for correct chamber attribution (which the old design got wrong) — not a strict win, and the write-back was corrected to frame this honestly rather than one-sidedly. The schema already supports the ideal fix for free (one politician row can hold multiple mandate rows across bodies); what's still missing is a CPF/voter-title-aware cross-body identity check at seed time, a real, documented, pre-existing gap this task relocates rather than resolves. With SENADOR now buildable and the other two leads closed/blocked-and-documented, all three of E2's named leads for Brazil are accounted for per the epoch's own stated exit language — though a REAL historical backfill re-run to actually populate SENADOR production data, plus the remaining ~28 election years for both bodies, remains the clear next increment.

2026-07-07 | STAGE 1 / br real SENADOR backfill write, 2018+2022 (commit d344840) | Re-ran the already-committed SENADOR-widen bins (no code changes, pure data population) against both years already covered for `DEPUTADO FEDERAL`. Seed: 1,890 new `Senado Federal` politicians (1,161/729), 329 same-pass collisions correctly refused, Câmara's 17,919 confirmed byte-unchanged. Real write (`BACKFILL_BUDGET=50000`): **1,337 new Senado filings, 12,672 new disclosure_records/outbox_events**. 2018's 758 published + 41 failed reconciles exactly against the ORIGINAL pre-widen run's own failure count (799) — those candidacies split cleanly into now-resolved vs. still-refused, nothing lost/duplicated. Alert-suppression and idempotency both exhaustively re-verified (independent audit ran an actual THIRD invocation of the real write, zero new writes, byte-identical DB snapshot). **Real finding, pre-existing, NOT introduced by this task**: while confirming Câmara stayed untouched, found and investigated 18 same-year multi-filing cases via CPF/voter-title cross-check against raw Bronze bytes — 17/18 are the same real person re-registered under a second candidacy (benign), but ONE is a genuine, currently-LIVE invariant-3 violation: **"JULIO CESAR DOS SANTOS" (BA, 2018, politician_id `01KWXE3M4J18YNCD5R1V7NTGQ3`)** is shared by two different real people (CPF `80673872653` vs `67701124500`) — independently confirmed via raw Bronze bytes with sha256 verification, and confirmed to predate this task (the affected rows' `storage_uri` still points at the old pre-Bronze-path-fix temp location). NOT fixed here (no deletion/merge attempted, per this session's own post-incident conservatism) — **flagged for a dedicated remediation task**. Critical scoping finding from the audit: this defect CANNOT self-heal via a plain re-run — `pipeline_run`'s parse/publish idempotency claims are keyed by content sha256 + a shared adapter code, not by `regime_id`/`body`, so a future invocation under the correct Senado binding hits `Claim::Replay` and silently no-ops without ever re-resolving. Remediation needs either a targeted claim invalidation or a manual invariant-1 superseding correction, not just another backfill run. `br` totals after this pass: 19,809 politicians, ~14,050 filings (12,119 Câmara + 1,337 Senado, plus the pre-existing local_br.rs proof), ~79,700 disclosure_records/outbox_events. Two full election cycles now cover BOTH Câmara and Senado — the remaining ~28 election years (back to 1994) plus the one known wrong-attribution case remain the clear next increments.

2026-07-07 | STAGE 1 / br identity-collision remediation, JULIO CESAR DOS SANTOS RESOLVED (commit 47f9c3c, execution this pass) | Full plan (`docs/decisions/br-identity-collision-remediation.md`, read-only, all 7 questions resolved by reading code/schema/live DB) → one-off bin (`crates/worker/src/bin/fix-br-julio-cesar-santos-ba-2018.rs`, narrowly scoped, dry-run by default) → independently pre-execution code-reviewed PASS → executed by rust-builder under an elevated review-before-run gate (the first politician-identity-split this codebase has performed). Sequence run exactly as planned: (1) final live dry-run re-confirmed the exact expected state (one collision, CPFs `80673872653`/`67701124500` on `01KWXE3M4J18YNCD5R1V7NTGQ3`, re-derived fingerprint matching stored `31924765f1...`) — nothing had drifted since planning; (2) `--execute` run once: minted politician `01KWY5XQ9SDYX7EHDD0AW0ZZSR` (mandate `01KWY5XQ9SRG0CSC38B3N1YE42`), repointed filing `01KWXEDGZQ8K0E9ZA75PB5C0ZS` and `disclosure_record` `01KWXEDGZR51RXG2QM5SHGEYW9` (fingerprint `31924765f1...`→`71fa7c68fc...`) in one transaction; (3) full plan §7 verification, all green: CPF-collision sweep now zero rows; both profiles complete and self-consistent (old id's 4 land/cash/car/savings records byte-identical to the script's own pre-write snapshot, zero changes; new id's 1 bank-deposit record correctly repointed); CPF re-verified directly against raw Bronze bytes (sha256 `83e06691b0...`→CPF `67701124500`→new id; sha256 `c7b7ce3052...`→CPF `80673872653`→stays on old id); fingerprint self-consistency confirmed (script's own Step 2 hard-halt re-derivation under the OLD baseline matched the live pre-write value, both in the dry-run and the real run; the printed Step 3 value matched the live post-write stored value via an independent DB query); blast radius exhaustive — `review_task` 0 matches (unchanged), `outbox_event`'s 5 pre-existing rows left deliberately stale under the OLD id per the plan (not a live query surface), both `pipeline_run` publish claims untouched/`succeeded`; br-scoped totals politicians/mandates 19809→19810 (+1 each, the new row), filings/disclosure_records 13466/79703 unchanged in count (a repoint, not an insert/delete) — no unexplained deltas anywhere; idempotency confirmed (second `--execute` invocation detected "already applied" via Step 0's live disambiguation and safely no-op'd, zero further writes); gates green (`cargo build/fmt/clippy -p worker -p br`, `conformance -- br` 3/3, unchanged). Deviation to flag honestly: the idempotency no-op was invoked one extra time beyond the plan's single authorized "second invocation" (re-checked exit code redundantly) — zero side effects, since the no-op branch is provably read-only (confirmed by source), but noted rather than silently omitted. Write-back: `docs/regimes/br/AUTHORITY.md` Quirks log entry appended marking the finding RESOLVED with full before/after ids/counts. `validate-survey -- br` to be re-run per this session's own established lesson (re-run after any phase appends to a committed regime artifact). NOT committed by this pass — held for orchestrator-managed post-execution audit, matching the rigor of every other real-write phase this session. The one known live wrong-attribution case this file's own JOURNAL/AUTHORITY entries flagged is now fixed; the general CPF/voter-title-aware cross-time/cross-body identity-resolution mechanism remains separate, unblocked future work.

2026-07-07 | STAGE 1 / br standing CPF-collision detection check (commit 52ad380) | Closed out the remediation plan's own final recommendation: `crates/worker/src/bin/check-br-identity-collisions.rs`, a small, strictly read-only bin using the SAME exhaustive sweep query (byte-identical, confirmed) that found and later verified the JULIO CESAR DOS SANTOS fix — now wired as a standing, report-only check so this defect class can't silently reappear at scale before the harder cross-time/cross-body identity mechanism exists. No write path, not gated into any CI/build step (confirmed via repo-wide grep — referenced only in documentation). Independently re-run against the live dev DB: `PASS: zero br politician-identity CPF collisions found` — confirming the fix fully resolved the one known case with nothing new since. Independently audited PASS (SQL match, read-only confirmation, no gate wiring, live re-run). This closes the identity-collision remediation arc end to end: found via due diligence → confirmed independently twice → planned read-only → built with a hard execute gate → reviewed before its first run → executed once → verified exhaustively → and now guarded against silent recurrence going forward.

2026-07-08 | 021 Phase 3 planning | Amended the Phase-2 consensus plan with the founder-approved hardening findings: design amendment 1 committed (A1-A17 + cross-lab AD, pol2/p2/q1 single-cutover, family-aware voting, bake-off numeric gates), 25-task hardening addendum H27-H47 committed atomically with surgical AMENDED edits to committed-plan Tasks 3/9/10/13/17/18/19/20/22/23/24/25 (Disputed carries RowKey + undeduped candidates; strict LlmConsensusRow letter-band schema + few-shot + doc-order prompt; Silver-shaped pg cache; single-slot premium disjunction; E1 stays v3->v4 once). Adversarially verified (3-lens review, 33-item fix wave, re-review gate). | HALTs carried: HARD CAP values (blocks H41b/H45/H46 spend); conditional non-Google vendor ToS (files goal at H45->H46 boundary). Phase-2 executor continues on the amended plan per the Merged Execution Order.
2026-07-09 | br | backfill:2014 | seed 6530 politicians nationwide (7744 discovered, 38 same-pass invariant-3 collision refusals, both bodies), real write 7737/7744 filings published, 7 failed closed (unresolved_filer, invariant 3 — the same 38-name collision set from seeding), idempotency re-run confirmed (0 published, 0 gold inserted, 7737 replayed) | 29338 new Gold rows (disclosure_record+outbox_event, all outbox pre-suppressed), 6530 new politician/mandate rows
FINDING (goal 092, not fixed here) — NEW identity collision, distinct from JULIO CESAR DOS SANTOS: `check-br-identity-collisions` post-2014-write sweep found politician_id `01KWXA32E7PMQ6D7CBEZJWCA9F` canonical_name "CARLOS ALBERTO DE SOUZA" carrying 2 distinct CPFs (`29168317972` 2014, `09867774809` 2022). Investigated read-only (no write): both filings are Câmara dos Deputados / DEPUTADO FEDERAL / SP (same body, same district/UF) — this is a CROSS-TIME collision, not cross-body: two different real people sharing name+UF+body+role, 8 years apart, resolved onto one politician row because `resolve_politician` matches only on `(alias, district, body)` with no year/CPF dimension. This is the exact architecturally-unguarded residual risk AUTHORITY.md/JOURNAL already document (see the 2026-07-07 SENADOR entry and the E2 scoping entry) — now empirically confirmed a SECOND time, this time purely from adding a new year rather than a new body. Per goal 092 scope, no fix attempted (flagged for the same future CPF/voter-title-aware cross-time/cross-body identity-resolution mechanism already tracked as open work). `check-br-identity-collisions` exit code 1 (REPORT, not PASS) as a result — expected given this finding, re-run after each subsequent year below to watch for further occurrences.

2026-07-09 | br | backfill:2010 | 0 rows, confirmed fail-closed: `seed-br-candidates --from 2010 --to 2010` failed closed at discovery (`bem_candidato_2010_AC.csv` CSV deserialize error, missing field `NR_ORDEM_BEM_CANDIDATO`) — the already-documented 2006/2010 `bem_candidato` column-layout mismatch (AUTHORITY.md Quirks log), reconfirmed live via the real bin, not re-derived from the prior sampler-only note. No code change (out of scope per goal 092); `backfill-real-br` NOT run for 2010 (would be a redundant second fetch of the identical TSE zip, invariant 10) | 0 new rows
2026-07-09 | br | backfill:2006 | 0 rows, confirmed fail-closed: `seed-br-candidates --from 2006 --to 2006` failed closed at discovery (`bem_candidato_2006_PI.csv` CSV deserialize error, missing field `NR_ORDEM_BEM_CANDIDATO`) — same documented 2006/2010 schema mismatch as 2010 above, reconfirmed live for a second year. No code change; `backfill-real-br` NOT run (redundant fetch, invariant 10) | 0 new rows
2026-07-09 | br | backfill:2002 | 0 rows, confirmed fail-closed: `seed-br-candidates --from 2002 --to 2002` failed closed at discovery — `bem_candidato_2002.zip` 404 Not Found, matching AUTHORITY.md's documented "no bem_candidato data this far back" class (same mechanism already proven for 2019/2021 non-election years). Spot-checked one year further (1998, not obligated by the goal but cheap): `bem_candidato_1998.zip` also 404, reconfirming the same gap class a second time — stopping per goal guidance, not chasing back to 1994/1933. No code change; `backfill-real-br` NOT run for either year (nothing to fetch a second time, invariant 10) | 0 new rows
2026-07-09 | br | backfill:2026 | 0 rows, confirmed NOT YET PUBLISHED (expected): `seed-br-candidates --from 2026 --to 2026` failed closed at discovery — `consulta_cand_2026.zip` 404 Not Found (the discovery-level candidate-registration file itself, not just bem_candidato). Consistent with today's date (2026-07-09) being ~3 months before the Oct 2026 general election; TSE has not yet published this cycle's data. Single conditional GET, concurrency 1 (invariant 10), no repeat probes. No write attempted (nothing available); this is an availability check only per goal 092 scope | 0 new rows
2026-07-09 | 093/Phase1 | politician-identity resolution mechanism built + both known br collisions closed (design doc `docs/decisions/politician-identity-resolution-design.md`) | 5-way scout survey (us_house+us_senate, uk_commons_register, canada_ciec, australia_register, eu_fr_de_annual) found: only `br`/`us_house`/`us_senate` currently call `roster.rs` in production; only `br` has a durable raw-source id (CPF, falls back to voter-title when CPF masked from 2024); uk/canada/eu/de already have durable ids (MNIS `member.id`, `clientId`, `mep_id`/`mdb_id`) sitting unused in Silver/details pending their own future RunnerBinding wiring (out of scope here); fr's `id_origine` isn't even plumbed into Silver yet. Built: migration `0013_politician_external_identifier.sql` (nullable, expand-only); `resolve_hits`/`resolve_politician`/`seed_roster` (`crates/pipeline/src/stages/roster.rs`) now exclude a hit whose stored `external_identifier` disagrees with the incoming one (catches JULIO CESAR's same-pass shape AND CARLOS ALBERTO's cross-pass shape via one mechanism), falling back to a 65-year tenure-plausibility check (justified against Dingell/Thurmond-scale real career lengths) when neither side has an id — explicitly documented as NOT a full solve for id-less regimes (can't catch an 8-year gap like CARLOS ALBERTO's real one). `br`'s `seed.rs`/`binding.rs` populate the id from CPF (sentinel `-4` aware) or voter-title at both seed and publish time. Zero behavior change proven for `us_house`/`us_senate` (`roster_historical.rs` byte-identical) and every pre-fix `br` row (all `external_identifier = NULL`, well inside the year-window's permissive fallback). New regression suite `pipeline/tests/roster_identity.rs` (4 tests, all green against live pg): same-pass split, cross-pass split (the CARLOS ALBERTO shape, live-DB-proven), legacy-row backward compat, implausible-gap fail-closed. **CARLOS ALBERTO DE SOUZA fixed**: `crates/worker/src/bin/fix-br-carlos-alberto-souza-sp.rs`, mirroring the JULIO CESAR template exactly (dry-run reviewed → `--execute`d once) — CPF `29168317972` (2014, 8 records) keeps `01KWXA32E7PMQ6D7CBEZJWCA9F`; CPF `09867774809` (2022, 3 records) moved to new politician `01KX3P9PVZK386AQPPMDD622QT`; fingerprints re-derived from raw Bronze and asserted set-equal pre-write; `check-br-identity-collisions` independently re-run PASS (zero); idempotency re-run confirmed no-op. `docs/regimes/br/AUTHORITY.md` Quirks log + `validate-survey -- br` re-run (OK) per this file's own established lesson. Full workspace green: fmt/clippy/test, all 8 adapters' conformance 3/3+5/5+4/4+5/5+7/7+4/4+9/9+1/1, full `--ignored` DB-gated suite. Incidentally found and fixed 2 small pre-existing issues blocking the gate that were NOT introduced by this pass: `crates/core/tests/migrate.rs`'s migration-count assertion was already stale (hardcoded 12, never bumped for the already-landed 0012 migration) — corrected to 14 (12+0013). Also found substantial unrelated, uncommitted, not-yet-green WIP sitting in the shared working tree (`crates/worker/src/migrate_local_to_prod.rs` + its bin/test, `pub mod` line in `worker/src/lib.rs` — reads as in-progress goal-081 "local→prod Gold migration" work, several real compile/clippy errors beyond a couple of quick fixes) — isolated it via pathspec-scoped `git stash`/`pop` (never touched via `git add -A`) to verify this session's own diff independently, then restored it byte-for-byte untouched for whoever owns that work to finish; NOT part of this goal, not committed by this pass. Filed as goal 093 (`agents/goals/093-identity-resolution-and-exhaustive-br-backfill.md`); Phase 2 (exhaustive br historical backfill, remaining years back from 2010) is the clear next increment, not started this pass.
2026-07-09 | 093/Phase2 | br 2006/2010 legacy bem_candidato schema built + 2010 real write | Downloaded and directly inspected the REAL 2006/2010 bem_candidato/consulta_cand bulk files (not inferred from error text, invariant 12): consulta_cand's roster-identity fields are byte-identical to 2014+; bem_candidato renames exactly 3 columns (`NR_ORDEM_CANDIDATO`→`NR_ORDEM_BEM_CANDIDATO`, `DT_ULTIMA_ATUALIZACAO`→`DT_ULT_ATUAL_BEM_CANDIDATO`, `HH_ULTIMA_ATUALIZACAO`→`HH_ULT_ATUAL_BEM_CANDIDATO`), everything else identical. Fixed with `#[serde(alias=...)]` on those 3 `BemCandidato` fields (`crates/adapters/br/src/parse.rs`) — no version dispatch needed, proven against all 81,050 real 2010 rows plus new inline regression tests (`adapter.rs`: legacy-header + modern-header-unchanged). Also checked candidatos-2002/1998/1994's FULL CKAN resource lists directly (not just the bem_candidato URL guess) — each carries exactly 3 resources (Candidatos/Coligações/Vagas, no asset-shaped resource under any name) — reconfirms invariant 12's "no bem_candidato data this far back" class is genuinely exhaustive, not just one 404. **2010 real write**: seed 4980 (88 same-pass collisions correctly refused), backfill 6245 published/26678 new Gold rows/42 failed closed (unresolved_filer, matching the same 42 same-pass-refused names), idempotency re-run confirmed (0 new). **Real Phase-1 gap found and closed**: `check-br-identity-collisions` surfaced 3 NEW collisions (`MARCOS ROBERTO DOS SANTOS` SP, `FRANCISCO DE ASSIS NUNES` SC, `JOSÉ CARLOS DOS SANTOS` RJ) — root cause: every pre-existing (pre-093) `br` politician has `external_identifier = NULL` by construction, so seeding a NEW year against the EXISTING roster always hits the weak year-window fallback, never the strong id-match path, exactly reproducing the defect Phase 1 fixed for *new* data on *old* data. Per goal instruction ("a NEW collision means Phase 1 has a gap — HALT back into Phase 1"), built `crates/worker/src/bin/backfill-br-external-identifiers.rs` (one-off, additive-only, dry-run/--execute): retroactively populated `external_identifier` for 16,399 pre-existing politicians with exactly one distinct CPF/voter-title across their filings (idempotent, re-run confirmed 0 remaining); correctly left the 3 already-ambiguous politicians untouched. Generalized the JULIO CESAR/CARLOS ALBERTO one-off-fix pattern into `crates/worker/src/bin/fix-br-cpf-collision.rs` (takes `--politician-id`, handles N filings per identifier group, deterministic tie-break) since a 4th+ occurrence crossed the predecessor bins' own "factor shared logic out once it recurs" threshold — dry-run reviewed then executed for all 3 (one hit a cross-worktree Bronze-file gap: the 2014 filing's Bronze bytes physically lived under the `front_b` worktree's own `target/`, not this one — copied, sha256-verified, source untouched). All 3 fixed; `check-br-identity-collisions` independently re-run PASS (zero); idempotency confirmed (re-run correctly halts rather than double-applying). Full workspace green (fmt/clippy/test, all packages, unrelated goal-081 WIP isolated via pathspec-scoped stash/pop as before). | 6245 new filings, 26678 new Gold rows, 3 collisions found+fixed, 16399+6 politicians gained a stored external_identifier
2026-07-09 | 081/T4 | BACKFILL_BUDGET skip: us_house 2024 record_delta=1459 exceeds budget=500 | none — nothing blocks; a later invocation retries 2024
2026-07-09 | 081/T4 | BACKFILL_BUDGET skip: us_house 2025 record_delta=2276 exceeds budget=500 | none — nothing blocks; a later invocation retries 2025
2026-07-09 | 081/T4 | BACKFILL_BUDGET skip: us_house 2026 record_delta=647 exceeds budget=500 | none — nothing blocks; a later invocation retries 2026
2026-07-09 | 093/Phase2 | br 2006 real write, cross-state SQ_CANDIDATO bug found+fixed | Seeding 2006 for real hit a MUCH higher same-pass collision rate than 2010 (4468/5487 vs 88/6530) — investigated instead of assuming "2006 just has more common names": direct inspection of the raw 2006 consulta_cand file found `SQ_CANDIDATO 10549` alone identifies 16 different real candidates across 16 different states (TSE evidently numbered candidates per-state before some later cycle). `BrAdapter::discover_year`'s `external_id`/`joined_cache` key and `assets_by_candidate` join both used bare `SQ_CANDIDATO`, so (a) every in-scope candidate sharing a colliding number appeared to share one identity — massive over-refusal, fail-closed but real candidates needlessly discarded — and (b) more seriously, a candidate's declared assets could silently get attached to an unrelated same-numbered candidate in another state via the `assets_by_candidate` collision, with NO fail-closed catch (this path never reaches identity_collision_counts). Fixed by keying both on `(SQ_CANDIDATO, SG_UF)` — confirmed unique for 2006 (same 40 residual duplicates whether or not cargo is included, all out-of-scope GOVERNADOR/VICE-GOVERNADOR ticket pairs); `binding.rs`'s `filing_identity()` updated to derive the identical `external_id` format the publish-time drift guard requires. **Verified 2010's already-published data (26678 Gold rows) is unaffected**: a direct check of the real 2010 file found zero SQ_CANDIDATO values shared across different states that year — TSE's numbering had already become nationally unique by 2010. After the fix: 2006 seed errors dropped from 4468 to 6 (the 6 being 3 genuine same-state same-name coincidences, correctly refused). Real write: 5485 filings published, 27498 new Gold rows, 2 failed closed (`LUIZ CARLOS DA SILVA` SP, the one in-scope same-name pair). Idempotency re-run confirmed (0 new). `check-br-identity-collisions` PASS (zero) — no new collision this time, both fixes (identity-resolution mechanism + cross-state join fix) working together correctly. Retroactively populated `external_identifier` for the 146 newly-seeded 2006 politicians (0 ambiguous). Full workspace green (fmt/clippy/test/conformance, unrelated goal-081 WIP isolated as before — that WIP has since landed and been marked done by its own owning session). This completes Phase 2's real-data recovery: 2014/2018/2022 (pre-existing) + 2010/2006 (recovered this session) are now real-written; 2002/1998/1994 confirmed exhaustively unrecoverable (full CKAN resource lists checked, no asset-shaped resource under any name); 2026 confirmed not yet published. | 5485 new filings, 27498 new Gold rows, 146 politicians gained a stored external_identifier, 0 new collisions
2026-07-09 | 093/audit | correction: 2010's "6245 filings published" figure was imprecise, not wrong — 2010 has exactly 3,842 filing rows | A completeness audit (parallel per-regime DB verification, prompted by "have we backfilled everything?") cross-checked every real-write claim against live row counts and found one apparent discrepancy: this file's own 2010 entries said "6245 filings published" but `select count(*) from filing where regime_id in (...) and split_part(external_id,':',1)='2010'` returns **3,842**. Investigated fully rather than shrugged off: re-ran 2010 discovery with the now-fixed code — unchanged at 6,287 in-scope candidates (confirms the earlier SQ_CANDIDATO cross-state fix does not apply to 2010, as already established); confirmed via `stg_br` that zero same-state OR cross-state `SQ_CANDIDATO` collisions exist for 2010 (ruling out any silent-attribution risk); then directly verified via a live re-fetch that of the 6,287 in-scope candidates, **2,403 declared zero assets** and 3,884 declared at least one — 2,403 + 3,884 = 6,287 exactly, and 3,884 − 42 (roster-resolution failures) = 3,842 exactly matches the DB. Zero-asset candidates are a legitimate, already-documented outcome (AUTHORITY.md/goal-092 "edge case 1" — a candidate may affirmatively declare no assets) that `publish_document` correctly handles by returning `Ok` with nothing to publish (invariant 6 compliant) — `backfill-real-br`'s own "published" counter increments on this success path too, conflating "processed without error" with "created a filing row." **Not a data-integrity bug**: no wrong attribution, no lost data, no fix needed — a reporting-precision correction only. (2006, for contrast, had zero zero-asset candidates that year — 5,487 discovered − 2 failed = 5,485 filed, exact match — real per-cycle variance, not a discrepancy.) | 0 rows changed; terminology correction only
2026-07-10 | memory+authority substrate v2 filed (founder-directed brainstorm + SOTA research) | design docs/plans/2026-07-10-memory-authority-substrate-design.md + goals 100-103 queued in 000-INDEX (100 authority lock/validator → 101 memory contract/index → 102 SAF normalization → 103 hygiene loop); loop implements | none
2026-07-10 | substrate Amendment 1 (founder-directed) | closed the root-CLAUDE.md coverage gap: design §6 omitted root CLAUDE.md, so sessions outside PROMPT.md/role loading (interactive, ad-hoc, other worktrees) got zero memory wiring. Landed now: root CLAUDE.md Memory section (load-matching-memory-first; write-back-is-done added to DoD; append-only/supersede; ` ```untrusted ` fencing) with interim wording until docs/memory/INDEX.md exists; new crates/adapters/CLAUDE.md SAF-pointer stub (subtree auto-load). Amended for the loop: goal 100 pins root /CLAUDE.md into AUTHORITY.lock (nested stubs explicitly unpinned — founder decision, pin-root-only); goal 101 gains root-wording finalization + nested subsystem stubs (crates/{api,pipeline,worker}, apps/web, infra); design doc Status line + §4.2 pinned set + §6 rows (dated Amendment 1, no renumbering); 000-INDEX rows 100/101 updated. Docs-only, no code touched | none
2026-07-10 | 097/Task7 | parallel-lanes-xhigh acceptance verified + merged to main | Full acceptance block re-run on `goal/097-parallel-lanes-xhigh` before merge: `cargo fmt --check` clean, `cargo clippy --all-targets -- -D warnings` clean, `cargo test --workspace` all green (0 failed; hit and retried past a transient linker-permission collision from another agent's concurrent build sharing this checkout's `target/` — confirmed via `tasklist` showing 4 live `cargo.exe` processes, not a real defect; re-ran the workspace test + the `--ignored` DB-gated suite under a private scratch `CARGO_TARGET_DIR` to avoid re-colliding, both fully green, 28 passed in `--ignored`), `sh -n` on run-loop.sh/monitor.sh clean, `/goal` grep 0 hits, `effort: xhigh` 11/11 shims, `jurisdiction-lease -- status` ran successfully against local Postgres (reported one real in-flight `br` lease, untouched — not this goal's to manage). Deliberately did NOT re-run the `GOVFOLIO_LANES=2 ./agents/run-loop.sh` acceptance line live: unlike the other checks, this one has a real side effect — it immediately launches a live, unattended `claude -p` orchestrator session with `--dangerously-skip-permissions` (real spend, real commits), not a dry-run. Task 5's already-recorded stubbed-`claude` smoke test is the evidence for lane startup/spawn mechanics; actually firing a real unattended loop is a founder call, not an acceptance-gate action. Merged `goal/097-parallel-lanes-xhigh` → `main` (--no-ff), pushed. | fmt/clippy/test/--ignored all green (28 passed --ignored), 1 real br lease found untouched, live-loop smoke deliberately not re-run (already proven stubbed in Task 5)
2026-07-11 | 100+104+108+109/implementation | autonomous Claude-Codex loop hardening and governed Codex skill dispatch complete | Rust supervisor owns all provider processes with singleton/lane fences, structured classification, bounded circuits/failover/evidence, exact skill canaries, zero-spend preflights, immutable Postgres receipts, generation-fenced leases, and singleton clean integration; raw Codex runner rejected in favor of a validation shim. Verified fmt, strict workspace clippy, isolated E2 role gate, workspace tests, offline local-Postgres ignored suite, migration safety, 39/39 Codex contract tests, clean detached contract verifier, all 38 adapter cases, OpenAPI zero-diff, web lint/typecheck/145 unit/26 e2e, and zero br identity collisions. | Release 2 live Codex+Claude canaries remain post-main-merge; Release 3 remains metric-gated
2026-07-11 | 021/live-canary-follow-up | fail-closed cross-lab disagreement observed during opt-in ignored test | With the inherited ANTHROPIC_API_KEY present, `us_house::extractor::live_extraction_agrees_with_ground_truth` rejected a Haiku/Sonnet disagreement across eight row fields; no data was accepted. Deterministic acceptance was rerun with external credentials removed and passed. | investigate model-output drift under open goal 021 before treating the live test as green
2026-07-11 | codex-agent-skill-contract | Added a fail-closed, manifest-driven skill contract for every root and nested Codex dispatch: 32 governed skill bridges, 11 native role shims, deterministic receipts, required-skill inheritance, trusted committed-input provenance, generated-artifact ownership, lane/worktree identity checks, and a contract-aware Codex loop. Independent adversarial review ended PASS after closing root-envelope, lane-local trust, symlink/junction escape, ambiguous routing, unsafe context, manufactured/staged input, relative-path loop, and assume-unchanged/TOCTOU bypasses. | Verification: contract 63/63, render/validator/clean-detached-worktree green; cargo fmt/clippy/workspace green; DB-backed ignored tests green through the external live-LLM gate, which correctly froze on repeated cross-model disagreement; web lint/typecheck/145 tests and contract drift green; Playwright 25/26 parallel with the lone keyboard-navigation flake green on isolated retry. Blockers: none for the Codex contract; live cross-model agreement remains external and fail-closed.
2026-07-11 | 110/postmerge-reconciliation | Reconciled the concurrent Codex skill contract and loop-board with the fenced provider-neutral supervisor. Retired the raw Codex retry loop, defaulted factory lanes to PROMPT-FACTORY-LANE.md, and prevented unrelated host Claude/Codex processes from creating repo-activity tripwires. | Contract/render/validator, fmt, strict workspace clippy, Release 2 filters, authority, isolated role gate (1020.52s), non-recursive workspace, local-Postgres ignored suite, web lint/typecheck/145 tests, and WSL status all green; live exact-main provider canaries remain the next bounded rollout action.
2026-07-11 | 110/PR3-guardrails | Linux CI exposed that the reconciled compatibility shim used lane-local contract scripts and inherited raw-runner assertions. Restored trusted-root policy hashing, linked-worktree/common-Git verification, and trusted root render/validate execution while preserving supervisor-only provider ownership; replaced raw Codex tests with zero-spend delegation proofs. | Contract suite 63/63, render/validator, detached-clean verifier test, and Git-Bash syntax green; PR CI rerun required.
2026-07-11 | 110/native-smoke-windows-path | The exact-main native smoke exposed a Windows-only Git boundary bug: canonical paths carried the `\\?\` verbatim prefix, which Git rejected while creating the disposable linked worktree. Child-process path serialization now removes only Windows verbatim drive/UNC prefixes; canonical `PathBuf` values remain intact for trust comparisons and cleanup. | Focused path and native-resolver tests green; exact native smoke and protected-main CI remain the rollout gates.
2026-07-11 | 110/Codex-live-canary-compatibility | The first exact-main Codex canary correctly failed closed twice against CLI 0.144.0-alpha.4: its stdout JSON omits model metadata, and Windows sandbox shell reads fell back to a completed `node_repl` file read that the verifier did not recognize. The verifier now resolves the exact completed thread's unique local rollout, requires matching session metadata plus one consistent model across all `turn_context` events, persists a compact hashed model proof, and accepts only completed/non-error `node_repl` reads that explicitly execute `readFile` against the exact governed skill path. Prose and incomplete tool claims remain rejected. | Real bounded canary PASS: Codex `gpt-5.6-sol`, official CLI SHA-256 identity, approved rust-tdd skill marker hash, structured load event, and exact-thread resume; focused fail-closed tests and strict supervisor Clippy green.
2026-07-11 | 110/Claude-live-canary-compatibility | Claude's exact-main canary reached the skill-evidence gate with the correct `claude-opus-4-8[1m]` init model and a genuine structured `Read`, but the verifier serialized the tool input before matching it; JSON escaping doubled Windows separators and hid the governed relative path. The matcher now walks decoded JSON string values recursively, normalizes separators there, and never treats object keys or prose as path evidence. | Real bounded canary PASS: Claude 2.1.207, exact configured/init model, approved rust-tdd skill marker hash, structured absolute-Windows `Read`, and exact-session resume; focused matcher tests and strict supervisor Clippy green.
2026-07-11 | 110/release2-closeout | Release 2 rollout gates are complete on protected main: native Codex linked-worktree/Git-common/rustc smoke passed on Windows, so WSL was not installed; Codex and Claude each proved exact configured model, structured loading of the tracked rust-tdd skill, the approved marker hash, and exact thread/session resume. | Codex proof `sha256:9161dc75557a0cb5b1a03d48acd24bfe579ba783647f8a14b1dde086b5754ced`; Claude proof `sha256:b56183de7f91f1d739eefc3395635419cb7627567e3c5a0353a004ea145a3a9a`; goal 110 complete, R3 remains metric-gated.
2026-07-11 | 112/root-dispatch-runtime-contract | The first exact-main supervisor launch failed closed before goal selection after exposing three root-runtime gaps: raw coordinator prompts bypassed the governed dispatch envelope, prebuilt authority/loop tools were stripped from provider environments, and simultaneous lane startup shared one compiler-canary cache. The supervisor now renders the trusted orchestrator dispatch before reservation/spawn (factory adds `trigger:parallel-work`), records the exact expected root `SKILLS_LOADED` receipt in every governed attempt, rejects missing/whitespace-mismatched receipts and any disallowed tool action before the exact receipt, preserves that boundary during cross-provider recovery, explicitly injects only the required prebuilt runtime paths plus epoch through the provider allowlist, and hashes lane identity into compiler-canary cache paths. | Independent review caught and closed late-receipt and recovery-prefix bypasses before publication. Resolver smokes for lane 0 and factory emitted deterministic governed envelopes; contract 63/63, fmt, and diff hygiene are green. Local Rust execution is temporarily host-blocked because this desktop sandbox cannot execute `gcc.exe`/`dlltool.exe`; protected CI is the required compile/clippy gate before merge, followed by a bounded exact-main relaunch.
2026-07-11 | build-test-performance / implementation | Removed the role scorer's recursive Cargo/fmt/Clippy/workspace/conformance execution and private `target/role-evals-nested`; RustBuilder calibration is now a pure fail-closed read of the frozen lock plus recorded goal/JOURNAL evidence, while current-code checks remain exact-commit release gates. The role suite is 14/14 in 0.08-0.18s versus the prior 174.74s nested phase; E2 remains OPEN at 7/7 roles scoring 1.00. CI now records stable per-gate start/end/duration/result, toolchain/SHA/profile and cache status, shares one Rust/DB dependency-cache namespace with DB restore-only, retains every legacy check during parity, and aggregates the four required jobs in a non-compiling fail-closed `release-gate`. Added byte-exact OpenAPI shadow proof, optional worktree-private Windows `sccache` wrapper, shadow-only nextest (zero retries, doctests separate, SQLx packages capped at concurrency 4), and a copy+count+SHA-256 Bronze migration away from disposable targets; no old Bronze was moved or deleted. Fresh Windows exact-tree baseline: `cargo test --workspace` 1010.693s total, Cargo compile 229s, fresh target 33.08 GiB; strict Clippy 97s; retained OpenAPI dev build 53.59s; CI-mode ignored SQLx/network inventory 349.338s warm. The large gap between Cargo's compile line and total wall time confirms severe local test-binary startup/scanning overhead, but Defender causality remains unproven, so exclusions and shared mutable targets remain rejected. An inherited-key live LLM run again failed closed on Haiku/Sonnet row-count disagreement after 715.877s; rerunning the identical ignored inventory with the optional key absent (documented CI mode, loud self-skip) passed, including the real network backfill test. Authority lock advanced to v4 under founder-approved goal-016 semantics. | Five clean CI parity runs, cache-hit telemetry, nextest inventory/verdict parity, failure injections, and required-status cutover remain rollout work; do not remove legacy OpenAPI regeneration or make shadow jobs required before those thresholds. Local web install was not rerun because elevated dependency-write approval hit the session usage limit; commit-bound CI remains authoritative for unchanged web gates.
2026-07-12 | build-test-performance / lock-version-correction (goal 113 PR) | Superseding note for the "build-test-performance / implementation" entry above: it states "Authority lock advanced to v4"; the lock actually committed to main via PR #10 is v6 — origin/main had advanced the lock to v5 in the interim, so --write-lock on the merged tree produced v6. Recorded per supersede-not-edit (invariant 1). | No code change; authority lock on main is v6, validate-authority --ci PASS at PR-#10 merge.
